# 자산 나침반 — 작업 인수인계

새 세션에서 이어서 작업할 때 먼저 읽는 문서. 사용자용 설명은 `README.md` 에 있다.

## 이 앱이 뭔가

가계부가 아니라 **재산 증가 내비게이션**이다. 메인 지표는 카테고리별 예산 소진율이
아니라 **순자산 대비 소비율**(`월 소비지출 ÷ 순자산`)이다. 같은 30만원도 순자산
1천만원인 사람과 3억인 사람에게 전혀 다른 의미라는 것이 출발점이다.

연환산 소비율이 4% 이하로 내려가면 자산 수익만으로 생활이 가능한 구간(4% 룰)이고,
앱은 거기까지 가는 경로를 계산해 "이번 달 얼마까지 써도 되는지"를 역산한다.

## 기술 제약 (바꾸지 말 것)

- **빌드 도구 없음.** `index.html` + `css/style.css` + `js/` 구조의 정적 사이트다.
  GitHub Pages 에 그대로 배포된다. React·TypeScript·번들러를 도입하지 않는다.
- **외부 런타임 의존성 0개.** 차트는 전부 직접 만든 인라인 SVG다.
- **데이터는 localStorage 에만.** 서버도 계정도 없고 네트워크로 나가는 데이터가 없다.
  코치 기능도 외부 API 없이 규칙 엔진으로 동작한다. 이건 금융 앱으로서의 핵심 약속이다.
- ES 모듈을 쓰므로 `file://` 로 열면 동작하지 않는다. 로컬 서버가 필요하다.

## 구조

```
index.html            앱 셸 (상단바, 네비, #main, 모달/토스트 루트)
css/style.css         디자인 토큰(CSS 변수) + 컴포넌트. 다크/라이트 2벌
js/
  format.js           원화·억/만 축약·개월수·날짜 포맷
  store.js            상태 + localStorage 영속화, 분류 체계, 리비전 카운터
  finance.js          지표·한도·목표배분·부채상환·예측·경고 엔진
  coach.js            규칙 기반 코칭 11종, 기회비용 환산
  charts.js           SVG 차트 (gauge, ring, spark, lineChart, donut, hBars, vBars, progress)
  ui.js               모달·토스트·폼 헬퍼
  seed.js             예시 데이터
  app.js              라우팅 · 액션 · 모달 배선
  views/              dashboard · assets · spending · goals · coach
sw.js                 오프라인 캐시 (네트워크 우선)
test/                 브라우저 검증 스크립트 (test/README.md 참고)
```

**렌더링 방식**: 각 뷰가 HTML 문자열을 반환하고 `app.js` 의 `render()` 가
`main.innerHTML` 에 통째로 넣는다. 이벤트는 `document.body` 에 위임 등록하고
`data-act` / `data-nav` / `data-tab` 속성으로 분기한다. 뷰에는 리스너를 달지 않는다.

**상태 변경**: `commit()` 을 부르면 리비전이 오르고 구독자(=`render`)가 호출된다.
`commit({ silent: true })` 는 저장만 하고 리렌더하지 않는다.

## 계산에서 반드시 지켜야 할 것

자세한 수식과 근거는 아래에 요약했다. 이 규칙들은 실제로 버그를 밟고 나서 정한 것이라
"단순하게 고치자"는 생각으로 되돌리면 같은 문제가 재발한다.

### 월말 지출 추정은 고정비/변동비를 나눈다

단순 페이스(`지출 ÷ 경과일 × 일수`)는 **월초에 폭주한다.** 1일에 주거비·보험료가
한꺼번에 빠지면 그 하루치가 30배로 곱해진다. 개발 중 실제로 월말 예상이 1,700만원으로
계산된 적이 있다.

```
고정비 추정 = max(이번 달 실제 고정비, 과거 3개월 평균 고정비)
변동비 추정 = 지금까지 쓴 변동비 + (남은 일수 ÷ 총 일수) × 과거 3개월 평균 변동비
```

같은 페이스로 소비 중일 때 1일차부터 29일차까지 추정치가 과거 평균과 오차 0%로 일치한다.

### 카테고리 한도는 고정비를 인정하고 변동비만 조절한다

주거·통신·보험·구독은 이번 달에 줄일 수 없는 계약이다. 고정비에는 실제 지출을 그대로
인정하고 남은 예산만 **사용자 본인의** 실제 소비 비중대로 변동비에 나눈다.
일반 템플릿 비율(`constants` 의 `share`)은 기록이 없을 때만 쓴다.
이 방식으로 바꾼 뒤 예시 데이터 기준 경고가 7건에서 4건으로 줄었다.

고정비는 한도가 실제 청구액과 같게 잡히므로 늘 100% 근처다. 그래서 경고 임계값이
다르다 — 변동비는 100% 초과, **고정비는 110% 초과**에만 경고하고 "임박(85%)" 은 내지
않는다. 이걸 빼먹으면 매달 무의미한 알림이 뜬다.

### 목표 저축은 가처분의 70%까지만

목표가 공격적이면 필요 저축이 가처분 전액을 먹어 한도가 0원이 된다. 지킬 수 없는
숫자는 지표로서 쓸모가 없다. 상한이 걸리면 `goalNeedCapped: true` 가 나오고 UI 에서
"목표가 수입보다 앞서 있습니다"라고 알린다.

### 부채 상환

`current` 전략은 **추가 상환을 아예 쓰지 않는다.** "아무것도 안 하면 이렇게 됩니다"를
보여주는 기준선이라 추가 상환을 넣으면 비교가 무의미해진다.

잔액이 전월보다 줄지 않으면 `months: null`, `feasible: false` 를 반환한다.
이자가 상환액을 앞선다는 뜻이므로 UI 에서 명확히 알려야 한다.

### 기타

- `저축/투자`·`대출상환` 카테고리는 소비율 계산에서 제외한다(`CATEGORIES` 의 `skip`).
- 부채 상환액은 거래로 기록하지 않고 `debts[].minPayment` 로 관리한다. 둘 다 넣으면 이중 계산된다.
- 월 이율은 `(1 + 연이율)^(1/12) − 1` 이다. `연이율 ÷ 12` 가 아니다.
- `burnHistory` 의 `burn: null` 은 "기록 없음"이다. 0으로 바꿔 그리면 그래프가 곤두박질친다.
- 성장 점수의 소비율 항목은 로그 스케일이다. 선형이면 고소비 구간에서 변별력이 사라진다.

## 이미 밟은 함정 (다시 밟지 말 것)

- **이벤트 리스너 중복 등록.** `document.body` 위임과 별도로 `.topbar` 에도 리스너를
  달았더니 상단바 버튼의 액션이 두 번 실행됐다(모달 2개, 테마 토글 제자리).
  위임 리스너 하나로 통일할 것.
- **라디오 묶음(`seg` 필드)에서 선택값 읽기.** `querySelector('[name=x]')` 는 첫 요소를
  준다. `[name=x]:checked` 를 써야 한다. 투자성향·목표 우선순위·테마가 전부 저장이
  안 되고 있었다.
- **SVG 원호가 viewBox 를 벗어남.** `gauge()` 의 원호 끝점이 박스 밖으로 나가 아래
  텍스트와 겹쳤다. 각도를 바꾸면 최하단 y(`CY + R·sin(end) + 획/2`)를 다시 계산할 것.
- **문자열 치환 시 검증 없이 진행.** 롤백으로 사라진 변수명을 기준으로 코드를 고치려다
  아무것도 안 바뀐 채 넘어간 적이 있다. 스크립트로 코드를 고칠 때는 assert 를 걸 것.

## 미해결 문제

### 슬라이더가 드래그 중 실시간으로 반응하지 않는다

`render()` 가 `main.innerHTML` 을 통째로 갈아치우면서 **드래그 중인 슬라이더 노드가
사라져** 드래그가 끊긴다. 누른 순간 값이 한 번 튀고, 놓았다 다시 잡아야 움직인다.

홈의 목표 소비율 슬라이더(`data-burn`)만 예외적으로 잘 동작한다.
`app.js` 의 `tuneBurn()` 이 전체 리렌더 대신 **영향받는 숫자 서너 개만 직접 갱신**하기
때문이다.

**시도했다 되돌린 방법** (`3176d91`, 되돌림 `55579d4`):
전체를 최소 변경으로 갱신하는 morph 를 넣었다. 슬라이더 5종 모두 실시간 반응까지는
성공했지만 **다른 화면이 여러 군데 깨져서** 롤백했다. 화면 전체를 노드 단위로
비교·교체하는 방식이라 테스트 시나리오로 다 잡히지 않았다.

이 과정에서 확인한 사실 하나는 다음 시도에도 유효하다 —
**크롬은 range 입력에서 `input` 이벤트를 포커스 이동보다 먼저 쏘는 경우가 있어
그 순간 `document.activeElement` 가 `BODY` 로 보인다.** "지금 슬라이더를 조작 중인가"를
`activeElement` 로 판단하면 안 되고 `pointerdown` 에서 직접 추적해야 한다.

**권장 접근**: 렌더링 구조를 건드리지 말고, `tuneBurn()` 처럼 슬라이더별로 부분 갱신을
하나씩 손으로 추가한다. 대상은 `data-fc`(미래 예측), `data-sim`(시뮬레이션),
`data-whatif`(가정해보기), `data-act="extra-pay"`(상환 전략). 한 번에 하나씩,
`test/sliders.mjs` 로 확인하면서.

### 목표 그래프의 주황 점선에 라벨이 없다

홈 히어로의 적립 경로 그래프에서 세로 주황 점선은 **목표일 마커**인데 설명이 없어
알아볼 수 없다. `charts.js` 의 `spark()` 에 `markLabel` 을 추가하면 된다.
(`3176d91` 에 구현이 있었으나 슬라이더 건과 함께 롤백됐다)

## 되돌린 작업 (히스토리에 살아있음)

| 커밋 | 내용 | 되돌린 이유 |
|---|---|---|
| `a4ce151` | NAVI 포레스트/라임 팔레트 이식 | 이전 다크/파랑 디자인이 더 낫다는 판단 |
| `3176d91` | 슬라이더 morph + 그래프 라벨 | 다른 화면이 깨짐 |

복구가 필요하면 `git revert <되돌림 커밋>` 또는 `git show <원본 커밋>` 으로 꺼내 쓴다.
NAVI 디자인 자산은 `a4ce151` 에 CSS 토큰까지 통째로 들어있다.

## 검증

`test/README.md` 참고. 코드를 고쳤으면 최소한 `test/screens.mjs`(13개 화면 렌더 +
콘솔 에러 0건)는 돌릴 것. 이 앱에서 실제로 깨진 것들은 대부분 브라우저에서만 드러났다.

## 배포

GitHub Pages. 저장소 Settings → Pages 에서 브랜치를 지정하면
`https://ajs-bit.github.io/ajs-bit.github.com/` 로 서비스된다.
저장소 이름을 `ajs-bit.github.io` 로 바꾸면 루트 주소를 쓸 수 있다.
