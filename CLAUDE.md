# 자산 나침반 — 작업 인수인계

새 세션에서 이어서 작업할 때 먼저 읽는 문서. 사용자용 설명은 `README.md` 에 있다.

## 이 앱이 뭔가

가계부가 아니라 **재산 증가 내비게이션**이다. 메인 지표는 카테고리별 예산 소진율이
아니라 **순자산 대비 소비율**(`월 소비지출 ÷ 순자산`)이다. 같은 30만원도 순자산
1천만원인 사람과 3억인 사람에게 전혀 다른 의미라는 것이 출발점이다.

연환산 소비율이 4% 이하로 내려가면 자산 수익만으로 생활이 가능한 구간(4% 룰)이고,
앱은 거기까지 가는 경로를 계산해 "이번 달 얼마까지 써도 되는지"를 역산한다.

## 기술 제약 (바꾸지 말 것)

- **빌드 도구 없음.** `index.html` + `css/style.css` + `js/` 구조의 정적 사이트다.
  GitHub Pages 에 그대로 배포된다. React·TypeScript·번들러를 도입하지 않는다.
- **외부 런타임 의존성 0개.** 차트는 전부 직접 만든 인라인 SVG다.
- **데이터는 localStorage 에만.** 서버도 계정도 없고 네트워크로 나가는 데이터가 없다.
  코치 기능도 외부 API 없이 규칙 엔진으로 동작한다. 이건 금융 앱으로서의 핵심 약속이다.
- ES 모듈을 쓰므로 `file://` 로 열면 동작하지 않는다. 로컬 서버가 필요하다.

## 구조

```
index.html            앱 셸 (상단바, 네비, #main, 모달/토스트 루트)
css/style.css         디자인 토큰(CSS 변수) + 컴포넌트. 다크/라이트 2벌
js/
  format.js           원화·억/만 축약·개월수·날짜 포맷
  store.js            상태 + localStorage 영속화, 분류 체계, 리비전 카운터
  finance.js          지표·한도·목표배분·부채상환·예측·경고 엔진
  coach.js            규칙 기반 코칭 11종, 기회비용 환산
  charts.js           SVG 차트 (gauge, ring, spark, lineChart, donut, hBars, vBars, progress)
  ui.js               모달·토스트·폼 헬퍼
  seed.js             예시 데이터
  app.js              라우팅 · 액션 · 모달 배선
  views/              dashboard · assets · spending · goals · coach
sw.js                 오프라인 캐시 (네트워크 우선)
test/                 브라우저 검증 스크립트 (test/README.md 참고)
```

**렌더링 방식**: 각 뷰가 HTML 문자열을 반환하고 `app.js` 의 `render()` 가
`main.innerHTML` 에 통째로 넣는다. 이벤트는 `document.body` 에 위임 등록하고
`data-act` / `data-nav` / `data-tab` 속성으로 분기한다. 뷰에는 리스너를 달지 않는다.

**상태 변경**: `commit()` 을 부르면 리비전이 오르고 구독자(=`render`)가 호출된다.
`commit({ silent: true })` 는 저장만 하고 리렌더하지 않는다.

**부분 갱신 (슬라이더)**: 드래그 중에는 `main.innerHTML` 을 갈아치울 수 없다.
잡고 있던 슬라이더 노드가 사라지면 브라우저가 드래그를 놓아버린다. 그래서
슬라이더가 있는 탭은 **컨트롤 카드**와 **출력 영역**으로 나눠 두고, `input` 에서는
출력 영역만 `setHTML()` 로 갈아끼운다(`ui.js`). 컨트롤 카드 안에서 값이 바뀌는
라벨은 id 를 붙여 `setText()` 로 고친다. 드래그를 놓는 `change` 시점에 비로소
전체를 다시 그려 상단 알림 배지처럼 부분 갱신이 닿지 않는 곳을 맞춘다.

```
탭()  = 컨트롤 카드(슬라이더 + id 붙은 라벨) + <div id="…-out">출력()</div>
live() = setText(라벨…) + setHTML('…-out', 출력())
```

**출력 HTML 은 최초 렌더와 부분 갱신이 반드시 같은 함수를 써야 한다.** 양쪽에
따로 쓰면 조용히 어긋난다. 슬라이더를 새로 추가하면 이 패턴을 따르고
`test/sliders.mjs`(값이 따라오는가)와 `test/regions.mjs`(갈아끼운 자리가 멀쩡한가)를
둘 다 돌릴 것.

**갱신 대상을 손으로 짚는 방식은 빠뜨리기 쉽다.** 홈(`liveBurn`)은 슬라이더와 출력이
한 카드에 섞여 있어 카드째 다시 그릴 수 없고, 그래서 자리를 하나씩 짚는다. 실제로
게이지가 빠져 있어 드래그 중 숫자만 움직이고 게이지는 놓아야 따라왔다.
**목표 소비율은 게이지의 눈금 최대값(`max = target × 3`)까지 바꾸므로** 숫자만 고치면
안 된다. 가능하면 코치·목표·자산처럼 출력을 통째로 `setHTML` 하는 쪽을 택할 것.

## 계산에서 반드시 지켜야 할 것

자세한 수식과 근거는 아래에 요약했다. 이 규칙들은 실제로 버그를 밟고 나서 정한 것이라
"단순하게 고치자"는 생각으로 되돌리면 같은 문제가 재발한다.

### 월말 지출 추정은 고정비/변동비를 나눈다

단순 페이스(`지출 ÷ 경과일 × 일수`)는 **월초에 폭주한다.** 1일에 주거비·보험료가
한꺼번에 빠지면 그 하루치가 30배로 곱해진다. 개발 중 실제로 월말 예상이 1,700만원으로
계산된 적이 있다.

```
고정비 추정 = max(이번 달 실제 고정비, 과거 3개월 평균 고정비)
변동비 추정 = 지금까지 쓴 변동비 + (남은 일수 ÷ 총 일수) × 과거 3개월 평균 변동비
```

같은 페이스로 소비 중일 때 1일차부터 29일차까지 추정치가 과거 평균과 오차 0%로 일치한다.

### 카테고리 한도는 고정비를 인정하고 변동비만 조절한다

주거·통신·보험·구독은 이번 달에 줄일 수 없는 계약이다. 고정비에는 실제 지출을 그대로
인정하고 남은 예산만 **사용자 본인의** 실제 소비 비중대로 변동비에 나눈다.
일반 템플릿 비율(`constants` 의 `share`)은 기록이 없을 때만 쓴다.
이 방식으로 바꾼 뒤 예시 데이터 기준 경고가 7건에서 4건으로 줄었다.

고정비는 한도가 실제 청구액과 같게 잡히므로 늘 100% 근처다. 그래서 경고 임계값이
다르다 — 변동비는 100% 초과, **고정비는 110% 초과**에만 경고하고 "임박(85%)" 은 내지
않는다. 이걸 빼먹으면 매달 무의미한 알림이 뜬다.

### 목표 저축은 가처분의 70%까지만

목표가 공격적이면 필요 저축이 가처분 전액을 먹어 한도가 0원이 된다. 지킬 수 없는
숫자는 지표로서 쓸모가 없다. 상한이 걸리면 `goalNeedCapped: true` 가 나오고 UI 에서
"목표가 수입보다 앞서 있습니다"라고 알린다.

### 부채 상환

`current` 전략은 **추가 상환을 아예 쓰지 않는다.** "아무것도 안 하면 이렇게 됩니다"를
보여주는 기준선이라 추가 상환을 넣으면 비교가 무의미해진다.

잔액이 전월보다 줄지 않으면 `months: null`, `feasible: false` 를 반환한다.
이자가 상환액을 앞선다는 뜻이므로 UI 에서 명확히 알려야 한다.

### 기타

- `저축/투자`·`대출상환` 카테고리는 소비율 계산에서 제외한다(`CATEGORIES` 의 `skip`).
- 부채 상환액은 거래로 기록하지 않고 `debts[].minPayment` 로 관리한다. 둘 다 넣으면 이중 계산된다.
- 월 이율은 `(1 + 연이율)^(1/12) − 1` 이다. `연이율 ÷ 12` 가 아니다.
- `burnHistory` 의 `burn: null` 은 "기록 없음"이다. 0으로 바꿔 그리면 그래프가 곤두박질친다.
- 성장 점수의 소비율 항목은 로그 스케일이다. 선형이면 고소비 구간에서 변별력이 사라진다.
- **값이 없는 항목에 점수를 주지 않는다.** 각 항목이 못 재면 `measured: false` 로 0점이고,
  메인 지표(자산 대비 소비)를 못 재면 `ready: false` 다. UI 는 이때 숫자 대신 무엇을
  입력해야 하는지 보여준다. 예전에는 못 재는 항목을 넉넉한 기본값으로 때워서, 자산 하나만
  넣은 사용자가 **75점 '우수' · '완전자립'** 을 받았다. 배점 55점짜리 두 항목이 '데이터 없음'
  에 만점을 주고 있었고, 성실히 입력할수록 점수가 떨어지는 구조였다.
- **지출 기록이 없으면 소비율은 0%가 아니라 `null`(측정 불가)이다.** 0%로 두면
  `gradeBurn` 이 최고 등급(완전자립)을 돌려준다. `coach.js` 는 이미 `burnAnnual !== null`
  로 막고 있어서, 지표를 정직하게 만들면 잘못된 코칭도 함께 사라진다.
- `lineChart()` 의 축 눈금은 **라벨이 채워진 위치 중에서만** 고른다. 호출부는 12개월마다
  라벨을 넣고 나머지는 빈 문자열로 두는데, 데이터 길이 기준(`len/6`)으로 뽑으면 두 간격이
  어긋나 대부분 빈 칸에 떨어진다. 실제로 '부채 잔액 경로'는 138칸을 23칸마다 뽑다가
  눈금이 '2년' 하나만 남아 있었다.
  마지막 눈금은 그래프가 다루는 기간을 알려주므로 늘 표시하되, 직전 눈금과
  `stride × 0.6` 보다 가까우면 직전 것을 밀어내고 그 자리를 차지한다. 둘 다 그리면
  글자가 겹친다 (실제로 '18년'과 '20년'이 8px 겹쳐 있었다).

## 이미 밟은 함정 (다시 밟지 말 것)

- **이벤트 리스너 중복 등록.** `document.body` 위임과 별도로 `.topbar` 에도 리스너를
  달았더니 상단바 버튼의 액션이 두 번 실행됐다(모달 2개, 테마 토글 제자리).
  위임 리스너 하나로 통일할 것.
- **라디오 묶음(`seg` 필드)에서 선택값 읽기.** `querySelector('[name=x]')` 는 첫 요소를
  준다. `[name=x]:checked` 를 써야 한다. 투자성향·목표 우선순위·테마가 전부 저장이
  안 되고 있었다.
- **SVG 원호가 viewBox 를 벗어남.** `gauge()` 의 원호 끝점이 박스 밖으로 나가 아래
  텍스트와 겹쳤다. 각도를 바꾸면 최하단 y(`CY + R·sin(end) + 획/2`)를 다시 계산할 것.
- **늘어나는 SVG 안에 글자를 넣음.** `spark()` 와 `lineChart()` 는
  `preserveAspectRatio="none"` + CSS `width:100%` + `style="height:Npx"` 조합이다.
  높이가 못박혀 **세로 배율은 항상 1인데 가로만 컨테이너 폭에 맞춰 늘어난다.**
  그 안에 `<text>` 를 넣으면 글자가 가로로만 퍼진다. spark(viewBox 폭 320) 기준
  실측 왜곡은 뷰포트 360px 에서 0.93배, 420px 1.12배, 768px 2.21배, 1100px 2.51배였다.
  두 차트 모두 글자를 **HTML 로 얹도록 고쳤다** (`centerLabel()` · `.chartbox__lbl`).
  세로 배율이 1이라 viewBox 의 y 를 그대로 px 로 쓰고, 가로만 백분율로 준다.
  양 끝에서 잘리는 건 CSS `clamp()` 로 글자 절반 폭만큼 안쪽에 묶는다.
  **이 두 차트에 글자를 새로 넣을 일이 있으면 `<text>` 를 쓰지 말 것.**
  왜곡이 없는 `gauge`·`ring`·`donut`·`vBars` 는 `<text>` 를 그대로 쓴다.
- **문자열 치환 시 검증 없이 진행.** 롤백으로 사라진 변수명을 기준으로 코드를 고치려다
  아무것도 안 바뀐 채 넘어간 적이 있다. 스크립트로 코드를 고칠 때는 assert 를 걸 것.
  치환 뒤 결과도 눈으로 볼 것 — 실제로 백틱이 두 개 붙은 적이 있는데 문법 오류가
  나지 않아(뒤쪽 백틱이 짝을 맞춰버린다) `node --check` 를 통과했다.
- **`document.activeElement` 로 "지금 조작 중인 슬라이더"를 판단.** 크롬은 range 입력에서
  `input` 이벤트를 포커스 이동보다 먼저 쏘는 경우가 있어 그 순간 `activeElement` 가
  `BODY` 로 보인다. 클릭 위치가 값을 바꾸는지에 따라 갈려서 증상이 슬라이더마다
  들쭉날쭉했다. 이벤트 대상(`e.target`)을 그대로 넘겨받아야 한다 (`ui.js` 의 `setAttr`).
- **슬라이더 상한이 자기 값에 딸려 움직임.** 추가 상환액 슬라이더의 `max` 를
  `m.capacity` 로 잡았는데 `capacity` 는 추가 상환액을 이미 뺀 값이라, 오른쪽으로
  끌수록 상한도 같이 줄어드는 움직이는 과녁이었다. 상한은 그 슬라이더의 값과
  무관한 기준으로 잡을 것 (지금은 `capacity + extra`).

## 미해결 문제

지금 알려진 것 없음. 아래 두 건은 `e558963`·`7609bed` 에서 해결했다.

### (해결) 슬라이더가 드래그 중 실시간으로 반응하지 않던 문제

전체 morph(`3176d91`)로 시도했다 다른 화면이 깨져 되돌린 뒤, CLAUDE.md 의 권장대로
**슬라이더별 부분 갱신**을 하나씩 붙여 해결했다. 방식은 위 "구조 · 부분 갱신" 참고.
`data-burn`(홈) · `data-sim` · `data-fc` · `data-whatif` · `extra-pay` · `target-burn`
6종 모두 드래그 내내 값이 따라온다. `test/sliders.mjs` 로 확인한다.

되돌린 morph 와 무엇이 다른가 — morph 는 **화면 전체**를 노드 단위로 비교·교체해서
어디가 깨질지 예측할 수 없었다. 지금 방식은 갈아끼우는 자리가 뷰 코드에 id 로
명시돼 있어 영향 범위가 그 컨테이너 안으로 닫힌다. 기준 커밋과 13개 화면을 픽셀
비교했을 때 11개가 완전히 동일했다(나머지 2건은 의도한 변경과 토스트 페이드 타이밍).

### (해결) 목표 그래프의 주황 점선에 라벨이 없던 문제

`spark()` 에 `markLabel`(세로 마커선) 과 `targetLabel`(가로 목표선) 을 넣었다.
라벨을 주면 위쪽 `TOP` 만큼이 글자 자리로 빠지므로 **호출부에서 `height` 를 같이
올려야 한다** (홈 히어로는 56 → 68). 목표선 라벨은 선 **아래**에 붙인다 — 목표가
최댓값이면 선이 맨 위에 걸려서 위에 쓰면 마커 라벨과 겹친다.

기한 없는 목표는 `markAt` 이 `null` 이라 세로선도 라벨도 나오지 않는다. 이때는
`TOP` 도 잡지 않아 그래프가 예전 높이를 그대로 쓴다.

**라벨은 SVG 안이 아니라 HTML 로 얹는다** (`.chartbox__lbl`). 위 "늘어나는 SVG" 참고.

## 되돌린 작업 (히스토리에 살아있음)

| 커밋 | 내용 | 되돌린 이유 |
|---|---|---|
| `a4ce151` | NAVI 포레스트/라임 팔레트 이식 | 이전 다크/파랑 디자인이 더 낫다는 판단 |
| `3176d91` | 슬라이더 morph + 그래프 라벨 | 다른 화면이 깨짐 (다른 방식으로 재구현 완료) |

복구가 필요하면 `git revert <되돌림 커밋>` 또는 `git show <원본 커밋>` 으로 꺼내 쓴다.
NAVI 디자인 자산은 `a4ce151` 에 CSS 토큰까지 통째로 들어있다.
`3176d91` 의 morph 는 **다시 꺼내 쓰지 말 것** — 같은 목적을 부분 갱신으로 달성했다.

## 검증

`test/README.md` 참고. 코드를 고쳤으면 최소한 `test/screens.mjs`(13개 화면 렌더 +
콘솔 에러 0건)는 돌릴 것. 이 앱에서 실제로 깨진 것들은 대부분 브라우저에서만 드러났다.
슬라이더나 부분 갱신을 건드렸으면 `sliders.mjs` 와 `regions.mjs` 도 함께 돌린다.

화면이 깨졌는지 확인하려면 **기준 커밋과 스크린샷을 픽셀 비교**하는 게 확실하다.
고치기 전 `git stash` 상태에서 `OUT=…/baseline node test/screens.mjs` 로 한 벌 찍어
두고 비교한다. 단, `shot-02-자산-자산` 은 예시 데이터 토스트가 페이드 중이라 같은
코드로 두 번 찍어도 그 부근(y≈770-810)이 달라진다. 차이가 거기뿐이면 무시해도 된다.

## 배포

GitHub Pages. 저장소 Settings → Pages 에서 브랜치를 지정하면
`https://ajs-bit.github.io/ajs-bit.github.com/` 로 서비스된다.
저장소 이름을 `ajs-bit.github.io` 로 바꾸면 루트 주소를 쓸 수 있다.
