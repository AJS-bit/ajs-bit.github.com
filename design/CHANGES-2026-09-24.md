# 2026-09-24 — 홈 히어로 게이지는 지금까지 쓴 돈 · 달력에서 적는다

사용자 피드백: 홈 히어로의 게이지가 월말 예상이라 「지금 얼마나 썼는지」가 보이지 않고, 달력 카드가 「여기서 적는 곳」으로 읽히지 않는다.
전 · 후 그림을 보고 승인 — 게이지 https://claude.ai/artifact/1KhE4xZCngC3dyAv2pfUpM · 달력 https://claude.ai/artifact/X9bqAH616ZyB4pWHkDW33k.
지시: 「고칠 때 지금 적용되어 있는 애니메이션, 관련된 UI와 수치, 유기적으로 연결된 것 다 고려해서 고쳐라. 하나만 고쳐 놓고 끝났다는 일처리는 없도록.」

**바뀌지 않는 것:** 계산식 · 엔진 · 저장 데이터 · 백업 형식 · 거래 ID · 반복 거래 · 보호 파일 · 서명 키. 새 값은 없습니다 — 이미 계산되는 두 값을 어디에 보여 주느냐만 바뀝니다.

| 이름 | 뜻 | 앱의 값 | 시안(9월 8일) |
|---|---|---|---|
| **쓴 돈** | 이번 달 지금까지 기록한 소비 합계(이체 · 대출상환 제외) | `model.current.spend` = `range.currentSpend` = `limit.used` | 1,120,000원 |
| **쓴 돈 비율** | 쓴 돈 ÷ 월 실수령 × 100 | `naviMetrics().salaryRatio` · 기록 부족 분기도 `salaryRatio(currentSpend, income, true)` | 31.1% |
| **월말 예상 비율** | 지금의 `salaryRatioProjected` | 배지 · 아래 칸 · 또래 카드 · 자산 한눈에 전용 | 57.9%(spendProjected 2,084,000) |

---

## 결정

### A. 홈 히어로 — 게이지는 지금까지 쓴 돈, 월말 예상은 글자로만

| 자리 | 내용 |
|---|---|
| 머리말 | `현재 위치`(모든 상태) |
| 배지(월말 예상 기준 · `salaryGrade(projected, target)`) | `월말에도 목표 안` 초록 ✓ · `월말엔 목표 초과` 주황(아이콘 없음) · `월말엔 월급 초과` 빨강(아이콘 없음). 폭 예약용 숨은 글자는 셋 중 가장 긴 문구. 예상 기준 통과 · 이번 달 기록 1건 이상일 때만 |
| 큰 숫자 | 쓴 돈 비율 소수 한 자리 + 작은 `%`. 0 < x < 0.05 이면 큰 `0.1` + 작은 `% 미만` |
| 설명 줄 | 쓴 돈 < 월급: `월급 {월 실수령} 중 {쓴 돈} 썼어요`(줄인 두 글자가 같아지면 쓴 돈을 원 단위로 — `월급 173만원 중 1,725,900원 썼어요`) · 쓴 돈 = 월급: `월급 {월 실수령}을 다 썼어요` · 쓴 돈 > 월급: `월급 {월 실수령}보다 {쓴 돈 − 월급} 더 썼어요`(`0원 더`는 없음 — 최종 점검 후속 4) |
| 오른쪽 줄 | **보이는** 반올림 값(소수 한 자리 · 움직이는 동안에도 그 프레임의 표시값) x < 목표: `목표까지` / `{차}%p 남음` 기본 글자색(ink — 초록 아님) · = 목표: `목표에` / `딱 닿았어요` 주황 · > 목표: `목표보다` / `{차}%p 넘음` 주황, x ≥ 100이면 빨강. 큰 숫자가 `0.1% 미만`이면 0보다 큰 값으로 본다 — 목표 0%면 `목표보다` / `0.1%p 미만 넘음`(최종 점검 후속 1). 한 줄 배치(데스크톱 · 좁은 폭) `목표까지 28.9%p 남음` |
| 게이지 | 채움 = 큰 숫자와 같은 값 · `내 목표 60%` 선과 축 끝 규칙(100% 넘으면 소수 한 자리 올림) 그대로 · 채움 색 그대로 · 스크린리더 `월급 대비 지금까지 쓴 비율` |
| 순자산 대비 | 쓴 돈 ÷ 순자산(`model.current.burn`) · 0 < x < 0.05 이면 `0.1% 미만` · 누르면 자산 탭 |
| 아래 세 칸 | `월 실수령` · `월말 예상` · 셋째 칸 이름만 `월말 예상 여유` / `월말 예상 초과`(전 `목표 초과`) — 값 · 색 그대로 |
| 각주 | 지금 규칙 그대로(고정비 예상 · 분류 안 함 · 한 달치) |

- **기록 부족(예상 기준 미통과) — 같은 틀.** 배지 없음 · 아래 칸 `월말 예상 —` · `월말 예상 여유 —` · 설명 줄 바로 아래 각주 두 줄: `아직 기록하지 않은 소비는 포함되지 않았어요` + 3종 `{M}월 기록도 확인하면 월말 예상을 볼 수 있어요 · 확인하기 ›` · `월말 예상에 쓸 이전 기록을 확인해 주세요 · 확인할 달 보기 ›` · `월말 예상에 쓸 지난 소비 기록이 아직 없어요`(`예상` → `월말 예상` · 링크 동작 그대로). 큰 숫자 · 게이지 · 오른쪽 줄 · 순자산 대비는 평소와 같은 규칙(쓴 돈은 사실이라 보류하지 않음).
- **이번 달 기록 0건**(`안 썼어요` 표시만 있어도 0건): 큰 숫자 자리 `아직 기록이 없어요` · 설명 줄 `달력에서 날짜를 눌러 쓴 돈을 적어 보세요` · 오른쪽 줄 · 순자산 대비 · 배지 숨김 · 게이지는 트랙 + 목표 선만(0%로 보이지 않게). 아래 칸은 상태 규칙대로.
- **월급 없음:** 큰 숫자 = 쓴 돈 금액(원 단위 54 + `원` 25) · 0건이면 `아직 기록이 없어요` · 설명 줄 `월급을 넣으면 월급의 몇 %를 썼는지 보여요` · 오른쪽 줄 · 순자산 대비 숨김 · 게이지 트랙 + 목표 선 · 배지 회색 `입력하면 보여요`(앱 문구 · 후속에서 시안도 맞춤 — **예상 기준을 통과한 달에만**, 기록 부족이면 기록 부족 틀대로 배지 없이 각주 두 줄 · 최종 점검 후속 19) · `월급 입력하고 시작` 그대로. 급여 0원 · 부수입만 있는 달도 같은 히어로이고 총수입 대비는 그 아래 대안 지표로만(`EmptyStates` C — 대안 지표 상자는 아직 앱에 없음).
- **가정 소비 모드:** 지금 들어갈 길이 없음(`scenarioSpend`는 가정 카드 안에서만 바뀜). 동작은 바꾸지 않고, 이 모드에서는 지금처럼 예상/가정 값을 쓰며 이 명세의 문구를 적용하지 않음.
- **목표 조정 창:** 끄는 동안 큰 숫자 · 게이지 채움 · 설명 줄 · `월말 예상` 칸은 그대로(좌표 · 글자 불변 — 기존 QA 규칙 유지), 목표 선 · `내 목표 N%` · 오른쪽 줄 · 배지 · 셋째 칸 이름만 초안 목표로. 잠금 줄 — 평소 `쓴 돈 112만원(31.1%)과 월말 예상 208만원은 목표를 바꿔도 그대로예요` · 기록 부족 `쓴 돈 N원은 목표를 바꿔도 그대로예요` · 0건 `소비 기록은 목표를 바꿔도 그대로예요`. 저장 미리보기 문장(한도 기반)은 그대로.
- **움직임(품질 빌드 규칙 그대로, 새 값에):** 첫 진입 — 큰 숫자 · 게이지 채움 · 설명 줄의 쓴 돈이 0부터 같은 450ms 진행률, 재진입은 게이지만 다시 차고 숫자는 바로 최종값. 값 바뀜(저장 · 수정 · 되돌리기) — 이전 표시값 → 새 값 240ms, 큰 숫자 · 게이지 · 설명 줄 금액 · 오른쪽 줄 · 순자산 대비가 같은 진행률, 형식이 바뀌는 숫자(원 ↔ 만 ↔ 억)는 트윈 없이 교체. 틀이 바뀌는 순간(0건 → 1건, 기록 부족 → 통과)은 움직임 없이 교체. reduced-motion은 전부 즉시.

### B. 같이 맞춘 곳

| 곳 | 바꿈 |
|---|---|
| 소비 › 이번 달 맨 위 카드 | 큰 숫자 = 쓴 돈 비율(31.1%) · 설명 `월급 대비 지금까지 쓴 돈` · 배지 = A의 세 이름(예상 기준 통과 · 기록 1건 이상) · 큰 숫자 옆 `잠정` 없앰, 기준 미통과 때는 그래프의 월말 예상 라벨에 `잠정`. 지난 달 화면은 그 달 실제 그대로 |
| 지난 달 비교 라벨 | `이번 달보다` → `이번 달 예상보다`(`SpendingPast` · 값 3.6%p 낮음 그대로) |
| 또래 카드 | 값은 월말 예상 그대로 · `내 소비율` → `내 월말 예상` · `목표 N% 안에서 순항 중` → `월말에도 목표 N% 안이에요` |
| 자산 한눈에 첫 줄 | 값 그대로(월말 예상 2.2%) · 이름 `순자산 대비 소비` → `순자산 대비 월말 예상` |
| 완료 카드 6순위 줄 | `소비율 57.9% → 58.2%` → `월급의 31.1% → 31.4%`(쓴 돈 비율 전 · 후) · 조건: 월급 있음 · 저장 전 이번 달 기록 1건 이상 · 전 · 후 글자가 다름(예상 기준 조건 삭제 — 뒤의 둘은 최종 점검 후속 9) · 우선순위 자리 그대로 |
| 첫 실행 안내 홈 1 | 본문 `이번 달 월급에서 지금까지 쓴 비율이에요. 막대가 내 목표 선을 넘지 않게 써 보세요. 월말 예상은 아래 칸에 있어요.`(제목 그대로) |
| 첫 실행 안내 홈 2 | 제목 `날짜를 누르면 바로 기록` 그대로 · 본문 `날짜를 누르거나 아래 + 버튼을 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요.`(후속 — 처음엔 `오늘 칸의 +나 「오늘 쓴 돈 적기」를 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요.`) |
| 데스크톱 옆 메뉴 | `소비 31.1%`(`DesktopHomeV5`) · `홈 31.1%`(`DesktopLedger`) |
| 소개 화면 | `IntroPosition` 31.1% · `월급 360만원 중 112만원 썼어요` · `월말에도 목표 안` · `목표까지 28.9%p 남음` · 순자산 대비 1.2% / `IntroRoute` `현재 31.1% · 목표 60%` |
| 홈 구성 · 설정 › 홈 구성의 고정 행 | `이번 달 달력` → `이번 달 소비 기록`(설명 `날짜를 눌러 바로 기록 · 늘 소비율 아래` 그대로) |

### C. 달력 카드 — 여기서 적는다

- 접힌 카드 제목 `이번 달 소비 기록`(펼치면 지금처럼 `‹ 2026년 9월 ›`). 머리줄 아래 안내 한 줄 `날짜를 누르면 그날 쓴 돈을 적어요`(12px · ink-3 · 접힘 · 펼침 둘 다 · 큰 글자 15px).
- 오늘 칸(이번 달을 볼 때): 늘 안쪽 테두리 1.5px brand. 오늘 기록 0건이고 `안 썼어요` 표시도 없으면 `—` 대신 파란 원 `+`(지름 20 · 큰 글자 24 · brand 바탕 · 흰 +). 기록이 있으면 금액 그대로. 다른 빈 날은 그대로 `—`. `+`가 금액으로 바뀌어도 칸 크기 불변.
- 오늘 기록 0건(안 썼어요 아님): 상태 문장 `오늘 아직 기록이 없어요` 대신 가득 찬 폭 버튼 `+ 오늘 쓴 돈 적기`(높이 40 · 반경 12 · brand-soft · brand 14/700 · 큰 글자 48 · 16.5). 누르면 오늘 하루 시트. 다른 안내 문장(저녁 `다 적었어요 ›` · 샘플 · 풀림 · 미래)은 버튼 위에 그대로.
- 오늘 기록이 있거나 `안 썼어요` 표시: 상태 문장 오른쪽에 알약 `+ 더 적기`(표시면 `+ 적기`) — 높이 32 · brand-soft · brand 13/700. 이 줄 전체 높이를 버튼과 같은 40(큰 글자 48)으로 — 저장 전후 카드 높이가 변하지 않음.
- 펼친 달력: 그 달 합계를 한 줄(`9월 기록한 소비 1,120,000원`)로 두고 그 아래 같은 버튼 / 알약 줄. 지난달을 보는 중이면 버튼 · 알약 없음(`8월 기록한 소비 1,715,200원` 한 줄). `확인할 내용 N개` 행 · 큰 글자 `목록으로 보기` 그대로.
- 접근성: 버튼은 button 요소 · 오늘 칸 aria-label 끝에 ` · 눌러서 적기`. 움직임: 누름 120ms(기존 규칙).

### D. 샘플 수치를 하나로

달력 9월 1일 합계 12,000 → **1,008,000원**. 9월 1~8일 합 = **1,120,000원** = 히어로의 쓴 돈(`sample-data.json` spendSoFar) = 한도 `사용 112만원` = 소비 탭 `112만`. 전에는 달력 124,000원과 히어로 112만원이 두 샘플 세계로 갈라져 있었습니다(`CHANGES-2026-09-20.md`). 8월 자료(1,715,200원)는 그대로입니다.

`LedgerV5` 9월 1일 행(12건 · 합 1,008,000원) — 9월 1~8일의 카테고리 합이 소비 · 한도 장의 값과 같게 나눴습니다(`gen_screens.py`의 assert가 지킴): 관리비 320,000 · 전기·가스·수도 150,000(주거/관리 47만 — 후속에서 월세 400,000 + 관리비 70,000을 바꿈, 아래 「후속」) · 보험료 120,000(보험 12만) · 인터넷·TV 80,000(통신 8만) · OTT·음악 35,500(구독) · 장보기 214,500(식비 24만) · 교통카드 충전 25,000(교통 7만) · 생활용품 13,000(쇼핑 3만) · 영화 10,000(문화/여가 1만) · 세탁소 28,000(기타 — 분류 안 함 32,000과 합쳐 6만) · 커피 4,500(분류 안 함) · 점심 7,500(식비). 내역 칩 `전체 24건`(전 14건) · 요약 `일반 소비 1,120,000원`. 소비 탭 그래프의 실제 누적선(`Spending` · `DesktopHomeV5`)도 달력 자료대로 다시 그렸습니다 — 1일에 고정비가 먼저 나가고(101만) 8일에 112만.

---

## 새 기준값

| 장면 | 쓴 돈 | 큰 숫자 | 설명 줄 | 오른쪽 줄 | 게이지 | 순자산 대비 | 아래 칸 |
|---|---|---|---|---|---|---|---|
| 평소 9월 8일(`HomeCalendarStrip` 등) | 1,120,000 | **31.1%** | `월급 360만원 중 112만원 썼어요` | `목표까지 28.9%p 남음` | `inset: 0 68.9% 0 0` | **1.2%**(112 ÷ 9,350) | 360만 · 월말 예상 208만 · 여유 8만 · 배지 `월말에도 목표 안` |
| 저장 뒤(`DoneCard` · 12,000원) | 1,132,000 | **31.4%** | `월급 360만원 중 113만원 썼어요` | `목표까지 28.6%p 남음` | `inset: 0 68.6% 0 0` | 1.2% | 월말 예상 210만 · 여유 6만(기존 값) · 완료 카드 6순위 줄이면 `월급의 31.1% → 31.4%` |
| 이력 부족(`HeroInsufficient` · 9/5 첫 실행 · 커피 5,000) | 5,000 | **0.1%** | `월급 360만원 중 5,000원 썼어요` | `목표까지 59.9%p 남음` | `inset: 0 99.9% 0 0` | **`0.1% 미만`**(0.005%) | 360만 · — · — · 배지 없음 |
| 같은 사람이 12,000원 더(`DoneCardStates` ②) | 17,000 | 0.5% | — | — | — | — | 완료 카드 `월급의 0.1% → 0.5%` |
| 급여 0원 · 부수입 120만(`EmptyStates` C · 예상 기준 통과) | 1,120,000 | `1,120,000원`(비율 없음) | `월급을 넣으면 월급의 몇 %를 썼는지 보여요` | — | 트랙 + 목표 선 | — | 배지 `입력하면 보여요` · `월 실수령 —` · `월말 예상 208만원` · `월말 예상 여유 —` · 그 아래 대안 지표 `93.3%` `총수입 대비 지금까지 쓴 돈`(112 ÷ 120 · 앱에 아직 없음) |
| 급여 미입력 · 지난 기록 없음(`EmptyStates` B · 기록 부족) | 1,120,000 | `1,120,000원` | 같음 | — | 트랙 + 목표 선 | — | 배지 없음 · 각주 두 줄(`… 포함되지 않았어요` + `월말 예상에 쓸 지난 소비 기록이 아직 없어요`) · 칸 `—` · `—` · `—` |

월말 예상 57.9%(spendProjected 2,084,000) · 월말 예상 208만원 · 순자산 대비 월말 예상 2.2% · 또래 비교 `기준보다 4.1%p 적게` · 지난달 비교 `3.6%p 낮음` · 계산 확정값(총이자 1,734만원 · 완제 2036년 4월)은 그대로입니다(`calc/crosscheck.py` 일치).

---

## 닿은 장 — 라이트 44 · 다크 43

캔버스에 있는 장은 라이트 42 · 다크 41이고, `Main` · `HomeScroll`(+ `DarkHome` · `DarkHomeScroll`)은 생성기가 잘라 쓰는 원본이라 캔버스에 없습니다. `Tokens`는 다크가 없습니다. 모든 다크 짝은 라이트와 같은 이유로 바뀌었습니다(`w()` 또는 `darken.py`).

| 장 | 무엇이 | 어디서 |
|---|---|---|
| `Main`(원본) | 히어로 — 배지 · 31.1 · `28.9%p 남음`(ink) · 설명 줄 · 1.2% · 채움 68.9% | 손편집 |
| `HomeCalendarStrip` · `HomeDefaultScroll` · `HomeConfigured` · `HomeStocksOff` · `HomeStocksCard` · `DoneCard` | Main 히어로 + 새 달력 카드(제목 · 안내 줄 · 오늘 칸 테두리 · `+ 더 적기` 줄) | `gen_v4` · `gen_v4_stocks` · `gen_v5` |
| `DoneCard` | 히어로 저장 뒤 값(31.4 · 113만 · 28.6%p · 68.6%) · 달력 `오늘 5건 49,000원` + 알약 · 본문 절단 22 → 27 | `gen_v5` |
| `HeroInsufficient` | 이력 부족 히어로를 Main 히어로에서 잘라 값만(0.1% · 5,000원 · 59.9%p · `0.1% 미만` · — · 각주 두 줄) · 달력 `오늘 1건 5,000원` + 알약 | `gen_v5` `hero_insufficient()` |
| `HeroFootnotes` | 왼쪽 온전한 카드의 히어로 · 부제 `월말 예상을 계산한 방식에 …` | `gen_v5` |
| `HomeCalendar` · `HomeCalendar360` · `HomeCalendarPrev` | 펼친 달력(안내 줄 · 9/1 `101만` · 그 달 합계 한 줄 + 알약 줄 · 지난달은 합계만) | `gen_calendar` |
| `CalendarCells` | 달력 · 오늘 칸 설명(⑤) · 끝 문단(테두리 1.5 / 누름 2) · 시작일 이전 견본의 번호 자리 38 → 59 | `gen_v5` |
| `CalendarGridSizes` | 카드마다 안내 줄 · 버튼 / 알약 줄 · 큰 글자 48 · 구현 메모 | `gen_v5` |
| `CalendarStatusLines` | 견본 6개(0건 = 오늘 칸 + · 버튼 / 안 썼어요 = `+ 적기` / 저녁 · 풀림 · 샘플 = 알약 / 미래 = 펼친 달력의 합계 줄 + 안내 + 알약) · 구현 메모 · 후속에서 견본 둘 더(펼친 달력 · 오늘 0건 / 처음 쓰는 날) | `gen_v5_sheets` |
| `DaySheetNoSpendStates` | 오늘 칸 견본에 테두리 · 후속에서 `+ → 0 → +` 견본과 문구 | `gen_v5_sheets` |
| `DoneCardStates` | 6순위 `월급의 31.1% → 31.4%`(그 밖에 · 월급을 넣었을 때) · ② 예상 기준 확인 전에도 `월급의 0.1% → 0.5%` · ③ 문구 · 메모 2 | `gen_v5_sheets` |
| `Spending`(→ `TourSpending1~3`) | 큰 숫자 31.1 · `월급 대비 지금까지 쓴 돈` · 배지 `월말에도 목표 안` · 실제 누적선 | 손편집 · `gen_tour` |
| `InsufficientElsewhere` | ① 큰 숫자 0.1(잠정 없음 · 그래프 라벨에만 잠정) · ⑥ 또래 · 메모 1 · 6 · 끝 문단 | `gen_v5_screens` |
| `FutureProvisional` | 메모 5(소비 · 이번 달의 잠정 자리) | `gen_v5_screens` |
| `SpendingPast` | `이번 달 예상보다` | `gen_rest` |
| `PeerStates` | A `월말에도 목표 60% 안이에요`(최종 점검 후속 — 전 `내 월말 예상 · 목표 60% 대비 2.1%p 여유`) · B `월말에도 목표 60% 안이에요` · C · D `내 월말 예상` | 손편집 |
| `HomeScroll`(원본 → `HomeDefaultScroll`) | 또래 `내 월말 예상` · `순자산 대비 월말 예상` | 손편집 |
| `LedgerV5`(→ `TourLedger1~3`) | 9월 1일 행 12건 · `전체 24건` · `일반 소비 1,120,000원` · 후속에서 주거/관리를 관리비 · 전기·가스·수도로(월세 없음) | `gen_screens` |
| `IntroPosition` · `IntroRoute` | 소개 히어로 · `현재 31.1% · 목표 60%` · 항로 바 31.1 | `gen_v4` |
| `HomeSetup` · `HomeSetupStocksOn` · `HomeLayoutEdit` | 고정 행 `이번 달 소비 기록` · 설명 `… 내 월말 예상 비교` · `순자산 대비 월말 예상 · …` | `gen_v4` |
| `TourHome1~3` | 문구 둘 · 달력 172 → 222 · 다음 안내 576 → 627 · 홈 2는 카드를 위로(아래에 두면 탭 바를 덮음) | `gen_tour` |
| `StockStates` | 가드레일 `월말 예상 소비율이 63.2%예요. 목표 60%를 넘어요.`(제안) | `gen_v4_stocks` |
| `EmptyStates` | B(월급 없음 — `현재 위치` · 1,120,000원 · 설명 줄 · 각주 · 트랙 + 목표 선 · 최종 점검 후속에서 기록 부족 틀로: 배지 없음 · 각주 두 줄) · C는 최종 점검 후속에서 예상 기준 통과 틀(칸 셋 · 기록 부족 각주 없음) · C(대안 지표 93.3% 쓴 돈 기준 · 후속에서 히어로를 B와 같게 · 배지 `입력하면 보여요`) · F(HeroInsufficient 히어로 그대로 · 둘째 줄 3종 · 0건 · 월급 없음 줄) | 손편집 |
| `DesktopHomeV5` | 히어로 · 옆 메뉴 31.1% · 또래 `내 월말 예상` · 그래프 누적선 · 늘 펼친 달력을 공용 조각으로 다시(안내 줄 · 합계 줄 · 알약) | 손편집 + `gen_calendar` |
| `DesktopLedger` | 옆 메뉴 `홈 31.1%` | 손편집 |
| `Components` | 02 배지 셋의 이름 · 08절(스트립 · 월 카드 · 칸 상태에 `오늘 · 안 적음` · 설명 1 · 2 · 3 · 4 · 10 · 이력 부족 히어로 조각) | 손편집 · `refresh_components_v5` |
| `Tokens` | display 견본 31.1% | 손편집 |

## 높이가 바뀐 장(세 곳 — 루트 · `gen_canvas.py` · `screens.json`)

실제 웹폰트(IBM Plex Sans KR)로 잰 자연 높이 + 여백 24 이상.

| 장 | 전 → 후 | 자연 높이 |
|---|---|---|
| `EmptyStates` | 2480 → 2560 → 2680(후속) → **2800**(최종 점검 후속) | 2766 |
| `Components` | 2724 → **2920** | 2896 |
| `DesktopHomeV5` | 1000 → **1070** | 1039 |
| `CalendarStatusLines` | 1050 → 1160 → 1500(후속) → 1540(최종 점검 후속) → **1570**(통합 점검 · 샘플 문장 위 줄) | 1543.5 |
| `CalendarCells` | 820 → **830** | 801 |
| `CalendarGridSizes` | 1490 → **1670** | 1639 |
| `InsufficientElsewhere` | 1330 → 1350 → **1390**(최종 점검 후속) | 1360 |
| `DaySheetNoSpendStates` | 1160 → **1190**(후속) | 1164 |

`DoneCardStates` 1090 → **1100**(최종 점검 후속 · 자연 1074) · `HomeDefaultScroll` 1330 → **1360**(최종 점검 후속 · 자연 1330). 폰 프레임(844)은 그대로입니다. 홈 달력 카드(접힘 · 확인할 내용 있음)는 171.5 → **222.4**, 히어로는 296 그대로입니다. `HomeDefaultScroll`은 그때 1330(자연 1298) — 최종 점검 후속에서 또래 각주가 두 줄 늘어 1360. 주식 단계의 두 홈(`HomeStocksOff` · `HomeStocksCard`)은 아래로 민 상태라 절단선을 다시 맞췄습니다(아래 여백 27 → 32 — 히어로 위 끝에서 약 105px, 큰 숫자 줄과 설명 줄 사이 빈 띠).

## 첫 실행 안내 — 홈 3단계(바뀐 표)

| 단계 | 강조 요소(실측 x, y, w, h · 모서리) | 제목 | 본문 |
|---|---|---|---|
| 1 / 3 | 히어로 (14, 90, 362, 296 · 20) | 여기가 현재 위치예요 | 이번 달 월급에서 지금까지 쓴 비율이에요. 막대가 내 목표 선을 넘지 않게 써 보세요. 월말 예상은 아래 칸에 있어요. |
| 2 / 3 | 달력 카드 (14, 395, 362, 222 · 20) · 카드 위 | 날짜를 누르면 바로 기록 | 날짜를 누르거나 아래 + 버튼을 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요. |
| 3 / 3 | 다음 안내 (14, 627, 362, 116 · 20) · 카드 위 | 지금 할 일 한 가지 | 기록이 쌓이면 가장 효과가 큰 일 하나를 골라 알려 줘요. 누르면 그 화면으로 바로 가요.(그대로) |

`CHANGES-2026-09-22.md`의 표에도 같은 값을 적고 전 문구를 괄호로 남겼습니다. 다른 탭 · 세그먼트 30장의 좌표는 그대로입니다(소비 · 내역 장의 카드 위치가 바뀌지 않음 — 다시 잼).

---

## 시안에서 정한 것(명세에 원문 없음 · 명세의 뜻에 맞춰)

1. **상태 문장 12.5px.** 승인 그림대로 11 → 12.5(알약 · 버튼과 나란히 읽히게). 큰 글자 16.
2. **펼친 달력의 합계는 따로 한 줄.** 명세는 「상태 문장 오른쪽에 알약」이라 한 문장(`9월 기록한 소비 … · 오늘 4건 …`)에 알약을 붙이면 두 줄이 되고, 버튼일 때(합계 줄 + 버튼)와 높이가 달라집니다. 그래서 펼침은 합계 한 줄 + `오늘 4건 37,000원 · + 더 적기` 줄 — 저장 전후 높이가 같습니다(명세의 「줄 높이 40으로 저장 전후 카드 높이 불변」의 뜻).
3. **큰 글자:** 안내 줄 15 · 버튼 48 / 반경 14 / 16.5 · 알약 38 / 15 · 오늘 칸 `+` 지름 24.
4. **스트립 칸 간격 3 → 2**(큰 글자는 3). 오늘 칸 안쪽 테두리(1.5px)에 요일 글자가 닿지 않게(56 안에 55 → 52). 〔최종 점검 후속 13 — 52로는 테두리와 요일 사이가 0.75px뿐이라 닿아 보였습니다. 수식어 줄을 12 → 9로 줄여 49 · 실측 2.27px(글꼴 상자) · 2.75px(화소).〕
5. **아직 오지 않은 날 안내**(3초)는 오늘 합계 자리를 대신하고 알약은 남깁니다(펼친 달력은 합계 줄 그대로).
6. **투어 홈 2의 카드는 달력 위.** 달력이 길어져 아래에 두면 탭 바를 덮습니다(629 + 186 = 815 > 778) — 투어 규칙 「아래에 자리가 없으면 위」.
7. **주식 가드레일 문구** `월말 예상 소비율이 63.2%예요. 목표 60%를 넘어요.` — 홈 큰 숫자와 같은 숫자로 읽히지 않게.
8. **`EmptyStates` C의 대안 지표도 쓴 돈 기준**(93.3% · `총수입 120만원 중 112만원 썼어요 · 월말 예상 208만원은 총수입을 넘어요`) — 홈 큰 숫자와 같은 뜻. 후속에서 C의 히어로 자체를 월급 없음(A)과 같게 바꿨고, 대안 지표 머리는 `대신 볼 수 있는 지표 · 월급이 아니라 총수입 기준`.
9. **`EmptyStates` B** 금액을 샘플 세계와 같게 124,000 → 1,120,000원 · 각주 한 줄 유지. 〔최종 점검 후속 19 — B는 앱이 실제로 그리는 기록 부족 · 월급 없음 틀(배지 없음 · 각주 두 줄)로 바꿈.〕
10. **`DoneCardStates` ②**를 HeroInsufficient 사람으로(커피 5,000 + 12,000 = 오늘 2건 17,000원 → `월급의 0.1% → 0.5%`).
11. **`Components` 칸 상태**에 `오늘 · 안 적음`(파란 +) 추가, 두 줄로 흐름.
12. **`LedgerV5` 9월 1일의 나눔**(위 D) · 소비 탭 그래프 누적선.
13. **또래 카드 A의 목표 줄** — 처음엔 `내 월말 예상 · 목표 60% 대비 2.1%p 여유`로 그렸으나 최종 점검 후속에서 명세 원문 · 앱 · B 견본과 같은 `월말에도 목표 60% 안이에요`로(`월말에도`가 월말 예상 기준임을 밝힘).

## 확인할 것

1. ~~투어 홈 2 문구와 그림~~ — 후속에서 닫음. 본문을 `날짜를 누르거나 아래 + 버튼을 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요.`로 바꿔 샘플의 오늘(기록 있음 · `+ 더 적기`)과 실제 첫날(0건 · `+ 오늘 쓴 돈 적기`)을 한 문장이 다 가리킵니다.
2. **0.1% 채움은 막대에 거의 보이지 않습니다**(0.14% = 0.5px). 명세대로 채움 = 큰 숫자이고, 최소 폭을 두지 않았습니다.
3. **옛 `DesktopHome`**(원본 전용 · 캔버스 · 등록부 · 체크리스트에 없음 · 생성기가 쓰지 않음)은 v3 기록이라 고치지 않았습니다. 데스크톱 홈은 `DesktopHomeV5`입니다.
4. 앱 샘플(`demoState(2026-09-08)`)은 쓴 돈 1,115,700원 → **31.0%**입니다. 시안 · 픽스처는 1,120,000원(31.1%)입니다 — 둘 중 하나로 맞출지는 앱 쪽 결정(코덱스 조사 `scope-app`).
5. **앱 샘플은 월말 예상 기준을 통과하지 않습니다**(최종 점검 후속 20). 샘플에 마감 기록이 없어 앱의 히어로 · 첫 실행 안내 홈 1은 늘 `월말 예상 —` · 배지 없음 · 각주 `월말 예상에 쓸 이전 기록을 확인해 주세요`입니다. `TourHome1`은 월말 예상 208만원 · `월말에도 목표 안`을 그립니다. 안내 본문 `월말 예상은 아래 칸에 있어요`는 두 경우 다 맞지만 그림과 앱이 다릅니다 — 샘플에 지난달 마감 기록을 넣을지는 4번과 함께 앱 쪽 결정.
6. **`DoneCard` 장은 분류 안 함 저장 뒤의 각주를 그리지 않습니다.** 앱은 `분류 안 한 N원은 적은 금액 그대로 예상에 더했어요`를 붙입니다(각주 규칙 그대로 · 시안 세계라면 32,000 + 12,000 = 44,000원). 홈 장은 각주 ①(덧붙일 말 없음)로 그리는 약속이고 규칙은 `HeroFootnotes`에 있습니다 — 그림은 그대로 둡니다.
7. **`SpendingPast`의 비교 줄 `이번 달 예상보다 3.6%p 낮음`은 앱에 없습니다.** v3부터 앱 지난달 화면에는 비교 줄이 없고, `마감 완료 · 확정` 배지 · `확정` · `남은 여유`(한도 − 소비) 칸 · `예상치 아님`도 앱(`마감값 기준` · `소비·상환 후`)과 다릅니다. 이름 바꿈(`이번 달보다` → `이번 달 예상보다`)은 시안에만 닿았습니다. 앱에 넣을지 시안에서 뺄지는 전 · 후 그림으로 따로 결정합니다(위 「앱에서 고칠 것」에서 `지난달 비교 라벨`을 뺐습니다).
8. **`EmptyStates` C의 대안 지표 상자는 앱에 없습니다**(`93.3%` · `총수입 대비 지금까지 쓴 돈` · 버튼 `총수입 기준으로 보기` — v3부터 백로그). 버튼 동작이 정해지지 않았습니다. 앱은 C에서도 월급 없음 히어로만 그립니다(칸 셋은 시안도 맞춤).
9. **`DesktopLedger`(+ 다크)는 v3 샘플입니다.** 날짜 행(9/3 210,000 · 9/5 ETF 30만 · 9/7 118,400 · 9/8 32,000 · 32건)과 반복 거래 5일이 달력 · `LedgerV5` · 같은 페이지의 `DesktopHomeV5`와 다릅니다 — 2026-09-21 결정 3-2A로 그대로 두고 구현 기대값으로 쓰지 않습니다(캔버스 데스크톱 페이지 노트에도 적음).
10. **앱 히어로의 `목표 조정` 알약(열면 `닫기`)과 한도 줄 `남은 N만원`은 어느 장에도 없습니다.** 목표 조정 창(잠금 줄 3종)은 장을 그려 전 · 후 승인을 받은 뒤 캔버스에 올립니다 — 그 전에는 홈 장에 넣지 않습니다. 한도 줄 이름 · 10원 단위는 3단계에서(`CHANGES-2026-09-21.md`).
11. **`PeerStates` A 부제는 v3 문구**(`20대 초반·중반·후반처럼 나눠서 알려 드려요.`)이고 앱은 연령 구간 설명 문장(`10년 단위 안에서 끝자리 0~3세는 초반…`)입니다 — 전부터 있던 차이 · 구현 기대값은 앱 문구.
12. **작은 화면 · 큰 글자의 첫 실행 안내 홈 1**(1.3배 · 높이 640 이하): 카드가 탭 바 위에 있어야 해서(17) 게이지와 아래 칸을 가립니다. 쓸어 올리면 보입니다 — 알려진 한계.

## 앱에서 고칠 것(코덱스)

- `app/page.tsx` — 히어로에 두 값을 나눠 넘김: 큰 숫자 · 게이지 · 설명 줄 · 오른쪽 줄 = `model.current.salaryRatio`(기록 부족 분기도 같은 식) · 순자산 대비 = `model.current.burn`(지금 `scenario.burnProjected`) · 배지 = `salaryGrade(salaryRatioProjected, target)` · 아래 칸은 그대로. 오른쪽 줄 색은 실제 비율 대 목표로 따로(지금 `tone` 재사용 금지). 데스크톱 옆 메뉴 `pctText(salaryRatio)`. `자산 한눈에` 첫 줄 이름. 또래 카드 라벨.
- `components/navi/home-hero.tsx` — 기록 부족 분기를 평소와 같은 틀로(머리말 · 원 단위 display · `월급의 N%` · `RouteBar value={null}` 제거), 배지 ✓는 steady만 · 숨은 폭 예약 글자를 새 이름 중 가장 긴 것으로 · 설명 줄 세 가지 · 0건 · 월급 없음 · `0.1% 미만` · 셋째 칸 이름 · 스크린리더 라벨. 움직임은 새 값에 같은 규칙(`MotionNumber` · `data-motion-graphic` · 설명 줄 금액 · 오른쪽 줄 · 순자산 대비 같은 진행률).
- `components/navi/home-target-editor.tsx` — 잠금 줄 3종.
- `components/navi/spending-overview.tsx` — 큰 숫자 쓴 돈 · 설명 · 배지 · `잠정`을 그래프 라벨로. (지난달 비교 줄은 앱에 없음 — 시안 `SpendingPast`만 바뀜 · 앱은 할 일 없음 · 「확인할 것」 7.)
- `components/navi/peer-card.tsx` — 라벨 둘(값 그대로).
- `lib/navi-commit-policy.ts` — 6순위 줄 `월급의 {before}% → {after}%`(`salaryRatio` · 조건 = 월급 있음 · 저장 전 이번 달 기록 1건 이상 · 전 · 후 글자가 다름 — 최종 점검 후속 9).
- `lib/navi-tour.ts` — 홈 1 · 2 문구(`scripts/check-first-run-tour.cjs`도). 홈 2 본문은 후속 문구 `날짜를 누르거나 아래 + 버튼을 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요.`.
- `components/navi/intro-flow.tsx` — 소개 화면의 예시 숫자와 문구를 새 히어로에 맞춤(소개 1 `31.1%` · `월급 360만원 중 112만원 썼어요` · `월말에도 목표 안` · `목표까지 28.9%p 남음` · 순자산 대비 1.2% / 소개 2 `현재 31.1% · 목표 60%` · 항로 바 31.1 — `IntroPosition` · `IntroRoute`).
- 홈 구성 고정 행 이름(`home-layout-editor.tsx`) · 달력 카드(`calendar-card.tsx`) — 제목 · 안내 줄 · 오늘 칸 테두리와 `+` · 버튼 / 알약 줄 40 · 지난달 없음 · aria-label. 다크의 `+`는 brand `#7FA0FF` 원 위에 글자가 surface `#121A2B`입니다 — 앱(`var(--surface)`)과 같고 시안도 `darken.py`가 흰색을 surface로 바꿔 같은 색입니다.
- `home-layout-editor.tsx` 안내 문장 `소비율과 달력은 늘 맨 위에 있어요.` → `소비율과 이번 달 소비 기록은 늘 맨 위에 있어요.`(후속 · `scripts/check-home-setup.cjs` 기대값도). 또래 기준 등록 창의 안내 `… 내 소비율과 바로 비교할 수 없어요` → `… 내 월말 예상과 바로 비교할 수 없어요`(`PeerDialog`).
- `lib/navi-quick-entry.ts` `statusLine` — 그 달 기록 0건의 합계 `{M}월 기록이 아직 없어요`, 안 썼어요 표시뿐인 달의 덧붙임 `안 썼어요 표시만 있어요`. 앱 작업본과 시안 · 계획이 같은 문구다(2026-09-24 맞춤).
- 테스트 · QA(코덱스 조사 `scope-app` 4절): `navi-v5-home.test.ts` · `navi-v5-hero-amount.test.ts` · `navi-v5-integration.test.ts` · `navi-v5-forecast-ui.test.ts` · `check-quality-target.cjs` · `check-ui-layout.cjs` · `check-quality-motion.cjs` 기대값. 계약서 `docs/v5-stage1-contract.md`의 이력 부족 히어로 절.

## 후속 — 독립 점검에서 나온 빈 곳(같은 날)

적용을 마친 뒤 독립 점검이 찾은 11가지를 모두 닫았습니다. 계산식 · 확정값 · 거래 자료의 합(9월 1일 1,008,000원 · 9월 1~8일 1,120,000원 · 카테고리 합)은 그대로이고 `calc/crosscheck.py`는 일치입니다.

| # | 빈 곳 | 고친 것 | 장 · 파일 |
|---|---|---|---|
| 1 | `DaySheetNoSpendStates`가 기록 0건인 오늘 칸을 `—`로 설명 | 오늘 칸은 파란 `+` — 안 썼어요를 누르면 `+ → 0`, 표시 풀기는 `0 → +`(이체가 있으면 `+` 아래 `이체`). 칸 견본을 전 → 뒤 두 칸으로, 소비도 이체도 없는 날에도 `+` 칸 | `gen_v5_sheets` · 1160 → 1190 |
| 2 | `EmptyStates` C(급여 0원 · 부수입만)가 옛 히어로(`—` · `월급 대비 소비율`) | 월급 없음(A)과 같은 히어로 — `1,120,000원` · `월급을 넣으면 월급의 몇 %를 썼는지 보여요` · 빈 트랙 + 목표 선 · 오른쪽 줄 · 순자산 대비 없음 · 배지 `입력하면 보여요` · 버튼 `월급 입력하고 시작`. 대안 지표 상자는 머리 `대신 볼 수 있는 지표 · 월급이 아니라 총수입 기준` + 앞 문장 `… 대신 총수입으로 나눠 볼 수는 있어요.`로 대안임을 밝힘(93.3% 그대로) · F의 월급 없음 줄 `위 B · C와 같아요` | 손편집 · 2560 → 2680 |
| 3 | 월급 없음 배지가 시안만 `급여 미입력` | 앱 · 명세 문구 `입력하면 보여요`로 — `EmptyStates` B · C · `SPEC-COMPONENTS` §4 · 위 A 월급 없음 · 계획 §11 | 손편집 · 문서 |
| 4 | 투어 홈 2 본문이 샘플 그림(오늘 기록 있음 · `+ 더 적기`)과 어긋남 | `날짜를 누르거나 아래 + 버튼을 누르고 숫자만 넣으면 끝이에요. 분류는 나중에 소비 탭에서 해도 돼요.` — `+ 버튼`이 두 모양을 다 가리킴(카드는 그대로 달력 위 · 두 줄) | `gen_tour` · `TourHome2` · 이 문서와 `CHANGES-2026-09-22.md`의 투어 표 |
| 5 | 홈 구성 안내 문장이 옛 이름 `달력` | `소비율과 이번 달 소비 기록은 늘 맨 위에 있어요.`(뒤 문장 그대로 · 한 줄에 들어가 높이 불변). 목록 설명 `내가 넣은 또래 기준과 내 월말 예상 비교` · `순자산 대비 월말 예상 · 자산 점수 · 10년 뒤 순자산`은 앱과 이미 같음 | `gen_v4` · `HomeSetup` · `HomeSetupStocksOn` · `HomeLayoutEdit` |
| 6 | 샘플 세계 충돌 — 9월 1일 행에 월세 400,000원, 그런데 `DaySheetConfirm` · `RecurringPrefill`은 9월 8일에 월세 700,000원을 적고 매달 8일 반복으로 등록 | 9월 1일 주거/관리 47만을 **관리비 320,000 · 전기·가스·수도 150,000**으로(행 수 12 · 합 1,008,000 · 카테고리 합 그대로 → `전체 24건` 그대로). `gen_screens`에 `월세`가 9월 자료에 없다는 assert 추가. 데스크톱 그래프 주석 `월세·보험 등 …` → `관리비·보험 등 고정비가 먼저 나갔어요`. `RecurringPrefill`(월세 · 700,000 · 결제일 8 · 시작 2026.10 · `이번 달 건은 방금 저장한 거래로 이미 있어요`)은 이제 9월 1일과 겹치지 않음 | `gen_screens` · `LedgerV5` · `TourLedger1~3` · `DesktopHomeV5`(손편집) · `sample-data.json` 주석 · 계획 §3-3 |
| 7 | `PeerDialog` 안내가 `내 소비율` | `… 내 월말 예상과 바로 비교할 수 없어요` | `gen_modals` · `PeerDialog` |
| 8 | `SPEC-COMPONENTS` §4 기록 0건 행에 안 썼어요 조건 없음 | `안 썼어요 표시만 있는 달도 0건으로 본다` · `EmptyStates` F 0건 줄에도 | 문서 · 손편집 |
| 9 | 안 썼어요 표시뿐인 달 · 기록 0건인 달의 상태 줄 | `안 썼어요 표시만 있어요`(전 계획 `표시만 있고 기록은 없어 소비율은 —예요`) · 펼친 달력 합계 줄은 `9월 기록한 소비 0원`이 아니라 `9월 기록이 아직 없어요` — `gen_calendar.month_total_text` · `MARKS_ONLY`(버튼일 때는 합계 줄 끝 · 알약일 때는 오늘 줄 끝 — 앱 `calendar-card`와 같은 자리) | `gen_calendar` · `CalendarStatusLines` 메모 · 계획 §3-3 · §8 · `SPEC-COMPONENTS` §27-3 |
| 10 | 계획 · 문서에 남은 옛 문장 | 계획: §7-2 · §11의 `이번 달 기록한 소비` 히어로 → 이력 부족 히어로(각주도 `월말 예상`) · §3-3 aria 템플릿에 오늘 칸 ` · 눌러서 적기` · §3-1 · §7-2 · 완료 기준의 달력 끄기 · 히어로 `소비 기록하기`를 지금 동작으로 고치고 옛 설계는 기록으로 · §3-4 이력 부족 문단의 12판 문장 둘에 〔12판 문장 — 13판이 대체〕 · §10 13판 메모에 후속 목록. `README` 원칙 1 · 5의 급여 없음 문장(A4대로) · `IMPLEMENTATION-PLAN` 3단계 홈 행 `DesktopHomeV5` · `CODEX-BRIEF`에 `preview/*.png`는 2026-09-10 v3 렌더이고 캔버스 장이 기준이라는 한 줄 · v4 계획 2026-09-24 메모 2에 안내 문장 | 문서 |
| 11 | 승인 그림(달력)의 두 상태를 생성기가 못 그림 | `calendar_card(today_empty=True)`(= `states={TODAY: NO_RECORD}`) · 펼침도 `states` · `since` · `review`를 따름 · 그 달 합계를 덮어쓴 칸을 빼고 다시 셈(`month_total`로 줄 수도 있음). `CalendarStatusLines`에 **펼친 달력 · 오늘 기록이 아직 없을 때**(`9월 기록한 소비 1,083,000원` + `+ 오늘 쓴 돈 적기` · 오늘 칸 `+` · 왼쪽 열 맨 아래)와 **처음 쓰는 날 · 시작일이 오늘일 때**(앞 6칸 날짜만 옅게 · 오늘 칸 `+` · 버튼 · 확인할 내용 행 없음 · 오른쪽 격자 7번째). 기본 인수의 결과는 예전과 같음(대조 확인) | `gen_calendar` · `gen_v5_sheets` · 1160 → 1500 |

- **오늘 칸의 `+`와 이체 배지가 함께일 때**(1번 견본에서 처음 나옴): `+`를 합계 자리(13px)에 겹쳐 놓고 수식어 줄은 그대로 둡니다 — 칸 안 높이가 다른 칸과 같은 52이고 요일이 테두리에 닿지 않습니다. 앱 `.calendar-cell-sum-add`(높이 13 · overflow visible)와 같은 방식이라 원이 날짜 · `이체`에 거의 닿습니다(`gen_calendar.plus_slot`). 〔최종 점검 후속 14에서 이 경우의 원을 16(큰 글자 20) · 글자 10(12)으로 — 날짜 1.94px · `이체` 1.37px 떨어짐.〕
- **다크의 `+` 글자색:** brand `#7FA0FF` 원 위에 surface `#121A2B` — 앱과 같습니다(`darken.py`가 흰색을 surface로 바꿈 · `SPEC-COMPONENTS` §27-1).

**높이(후속):** `CalendarStatusLines` 1160 → **1500**(자연 1469 — 왼쪽 열 1294 · 오른쪽 격자 1230) · `DaySheetNoSpendStates` 1160 → **1190**(자연 1164) · `EmptyStates` 2560 → **2680**(자연 2647). 루트 · `gen_canvas.py` · `screens.json` 세 곳 같음(`sync_screens.py` 통과). `HomeSetup` 843 · `HomeSetupStocksOn` 808 · `HomeLayoutEdit` 776 · `PeerDialog` 765 · `DesktopHomeV5` 1039 · `TourHome2`는 그대로입니다.

**후속에서 닿은 장 — 라이트 13 · 다크 13:** `DaySheetNoSpendStates` · `CalendarStatusLines` · `EmptyStates` · `HomeSetup` · `HomeSetupStocksOn` · `HomeLayoutEdit` · `PeerDialog` · `LedgerV5` · `TourLedger1~3`(9월 1일 행 — 폰 프레임 아래라 그림은 그대로) · `TourHome2` · `DesktopHomeV5`. 고친 생성기: `gen_calendar` · `gen_v5_sheets` · `gen_screens` · `gen_modals` · `gen_v4` · `gen_tour` · `gen_canvas`.

생성기 순서는 그대로입니다(`_tools/README.md`). 이 판에서 고친 생성기: `gen_calendar` · `gen_common`(`tag` 아이콘) · `gen_screens` · `gen_rest` · `gen_v4` · `gen_v4_stocks` · `gen_v5` · `gen_v5_sheets` · `gen_v5_screens` · `refresh_components_v5` · `gen_tour` · `gen_canvas`.

## 최종 점검 후속 — 독립 검증 뒤 마지막 정리(같은 날)

앱 작업본(`v5-stage1` 787e559 위의 작업 트리)과 시안을 한 번 더 독립 검증해 확인된 어긋남을 닫았습니다. 계산식 · 엔진 · 확정값 · 저장 데이터 · 백업 형식 · 거래 ID · 반복 거래 · 보호 파일은 그대로이고 `calc/crosscheck.py`는 일치입니다. 1~18은 앱이 따르는 규칙을 이 문서 · `SPEC-COMPONENTS.md`의 기준으로 적은 것, 19는 시안만 고친 것, 20은 고치지 않고 위 「확인할 것」 5~10에 적은 것입니다.

| # | 정한 것(기준) | 앱 쪽 자리 |
|---|---|---|
| 1 | **목표 0% · 쓴 돈 0.05% 미만.** 큰 숫자 `0.1% 미만`은 0보다 큰 값이라 오른쪽 줄은 `목표보다` / `0.1%p 미만 넘음`(주황) — `목표에 딱 닿았어요`가 아님. 목표가 0보다 크면 차는 지금처럼(목표 60 → `60.0%p 남음`) | `lib/navi-hero.ts` `targetLine` |
| 2 | **움직이는 동안 설명 줄은 보이는 값을 따른다.** 설명 줄의 모양(`중 … 썼어요` · `을 다 썼어요` · `보다 … 더 썼어요`)과 금액은 매 프레임 큰 숫자와 같은 진행률(첫 진입 450ms · 값 바뀜 240ms)의 표시값에서 나옵니다. `…보다 0원 더 썼어요`는 어느 프레임에도 없습니다. 오른쪽 줄의 종류와 색(주황 · 100% 이상 빨강)도 보이는 반올림 값으로. 원 ↔ 만 ↔ 억 형식 교체는 최종값 기준으로 트윈 없이 | `home-hero.tsx` |
| 3 | **카운트업은 최종 단위로.** 최종값이 만 단위면 지나가는 값도 만 단위 — 원 단위의 긴 글자로 설명 줄이 넘쳐 아래가 흔들리지 않게 | `motion.tsx` `MotionAmount` |
| 4 | **월급 바로 아래 · 딱 같을 때.** 쓴 돈 < 월급인데 줄인 두 글자가 같으면(173만 / 173만) 쓴 돈을 원 단위로 `월급 173만원 중 1,725,900원 썼어요`. `월급 {월급}을 다 썼어요`는 쓴 돈 = 월급일 때만(이때 큰 숫자 100.0% · `목표보다 40.0%p 넘음` 빨강) | `home-hero.tsx` · `lib/navi-hero.ts` |
| 5 | **히어로 배치.** 모바일 설명 줄과 `순자산 대비` 사이 10px(시안 `Main`과 같음) · 데스크톱 요약 폭 `* 11` · 칸 이름 nowrap(`DesktopHomeV5`) · 359px 이하 칸 좌우 안쪽 6px | `home-target.css` · `globals.css` |
| 6 | **100% 넘는 축 끝**(소수 한 자리 올림)은 값과 같은 트윈으로 움직임 — 저장 · 되돌리기 순간 채움 · 목표 선이 튀지 않음 | `home-hero.tsx` |
| 7 | 빈 칸 `—`는 ink-4(`#697182` · 다크 `#7E8AA2` — 시안과 같음) · `월말엔 목표 초과` 배지는 warning(`#B45309` on `#FDF1E0` · 다크 `#F5B544`) · 데스크톱 그래프 머리는 두 줄로 쌓고 keep-all(`누적 112만원 · 한도 216만원`이 금액 중간에서 끊기지 않게) | `shared.tsx` · `globals.css` |
| 8 | **이번 달 기록 0건이면 판정하지 않는다.** 코치 · 다음 안내 · 또래 카드 · 사실 줄의 판정 조건 = 예상 기준 통과 **그리고** 이번 달 기록 1건 이상(`안 썼어요` 표시뿐인 달도 0건). 0건이면 제목 `이번 달 기록이 아직 없어요` · 본문 `달력에서 날짜를 눌러 쓴 돈을 적어 보세요`, 사실 줄은 `이번 달 기록이 아직 없어요 · 달력에서 날짜를 눌러 쓴 돈을 적어 보세요`(`기록한 소비 0원` 없음). 월급이 있으면 월 수입을 묻지 않습니다. 또래 카드(월급 있음 · 0건): 제목 `이번 달 기록이 아직 없어요` · 띠 라벨 `이번 달 기록` · 버튼 `소비 기록하기` 하나(`급여 입력하기` 없음). 또래 카드 미입력 머리 `내 월말 예상` + `—`(스크린리더 `내 월말 예상 미측정`). 또래 기준 등록 창 안내 `실수령 급여가 아닌 다른 소득을 기준으로 한 통계라면 내 월말 예상과 바로 비교할 수 없어요. 출처의 기준을 꼭 확인하세요.` 다음 안내의 목표 초과 제목은 `월말 예상을 {N} 줄여보세요`(전 `월급 대비 목표까지 {N} 줄여보세요` — 앞선 적용에서 바뀐 것을 여기 적음) | `navi-insights.ts` · `navi-next-action.ts` · `navi-quick-entry.ts`(FACT_LINE) · `peer-card.tsx` |
| 9 | **완료 카드 6순위 줄이 없는 경우** — 월급 없음 · 저장 전 이번 달 기록 0건(그달 첫 기록 — `월급의 0.0% → …`를 보이지 않음) · 전 · 후 글자가 같을 때(`31.1% → 31.1%` · `0.1% 미만 → 0.1% 미만`) | `navi-commit-policy.ts` |
| 10 | **소비 탭 · 기록 0건.** 그래프 머리 `{M}월 기록이 아직 없어요`(전 `누적 0원`) · 오늘 점과 `0원` 라벨 없음 · 카테고리 목록에 `0원 · 0%`를 결과로 보이지 않음(`—` 또는 기존 빈 모양). 맨 위 카드의 잠정 문장은 뺌 — `잠정`은 그래프의 월말 예상 라벨에만(`InsufficientElsewhere` ①). `자산 한눈에` `순자산 대비 월말 예상`은 0 < x < 0.05면 `0.1% 미만` · keep-all | `spending-overview.tsx` · `spending-analysis.tsx` · `app/page.tsx` |
| 11 | **달력 안내 문장은 알약 옆.** `9월 8일 표시가 풀렸어요 · 다시 표시` · 3초 `아직 오지 않은 날은 적을 수 없어요`는 알약이 있을 때 오늘 합계 자리를 대신합니다(샘플 문장은 통합 점검에서 늘 위 줄로 — 아래 21) — 줄 높이 40(큰 글자 48) 그대로라 저장 전후 카드 높이가 같습니다. 펼친 달력의 그 달 합계 줄 · `확인할 내용 N개` 행은 그대로. 버튼(오늘 0건)일 때는 버튼 위 한 줄(시안 `CalendarStatusLines` 그대로) | `calendar-card.tsx` |
| 12 | **누르는 영역 · 포커스.** `확인할 내용` 행 위 간격 6(시안 값) — 행의 누르는 영역(위아래 6)이 버튼 · 알약을 덮지 않고 알약의 실제 누르는 영역은 44 이상. 하루 시트를 닫으면 포커스는 연 자리로, 그 자리가 없어졌으면 알약 · 버튼 · 오늘 칸으로 | `v5-calendar.css` · `day-sheet.tsx` `finalFocus` · `app/page.tsx` |
| 13 | **스트립 칸(보통 글자).** 간격 2 · 수식어 줄 **9**(12 아님) → 칸 안 10 + 2 + 11 + 2 + 13 + 2 + 9 = 49를 56 가운데에. 오늘 칸 테두리(1.5)와 요일 글자 사이 실측 2.27px(글꼴 상자) · 2.75px(화소) — 전 0.75. 칸 44 × 56 그대로. 큰 글자는 간격 3 · 수식어 13 · 자기 크기 그대로 | `v5-calendar.css` · `gen_calendar.STRIP_MOD_H` |
| 14 | **오늘 `+`에 이체 · ✓가 함께면 원 16(큰 글자 20) · + 글자 10(12).** 합계 자리에 겹쳐 놓아도 날짜와 `이체`에 닿지 않습니다 — 실측 날짜 1.94px · `이체` 1.37px(전 0.44 · 0.86 — 원 20). 월 달력 칸도 같은 규칙(날짜 4.1 · `이체` 2.2). 그냥 `+`는 20(큰 글자 24) 그대로 | `v5-calendar.css` · `calendar-card.tsx` · `gen_calendar.PLUS_WITH_MOD` |
| 15 | **큰 글자 크기 = 시안.** 안내 줄 15 · 상태 문장 16 · 알약 15(높이 38) · `확인할 내용` 행 44 / 16 · 오늘 `+` 24 · 버튼 48 · 반경 14 · 아이콘 18. 1.3배 글자에서 투어 강조 좌표를 다시 잼 | `v5-calendar.css` |
| 16 | **목록으로 보기의 오늘 행**도 칸처럼 brand-soft + 안쪽 테두리 1.5px, 오늘 기록 0건이면 `—` 대신 파란 `+`(24). 버튼 · 알약은 목록 아래 상태 줄 자리 그대로 | `calendar-card.tsx` · `CalendarGridSizes` 견본 |
| 17 | **첫 실행 안내 카드는 탭 바를 덮지 않는다**(탭 바 위 끝이 아래 한계 — 홈 2에서 3px 덮던 것). 좁은 배치(320 · 1.3배)에서는 카드 위로 게이지가 보이게 화면을 더 내림 | `first-run-tour.tsx` |
| 18 | 하루 시트의 `오늘` 칩은 안 썼어요 표시한 날에도 그대로. 홈 구성 창의 안내 · 스크린리더 글도 `이번 달 소비 기록`(`고정 달력` · `달력` 아님) | `day-sheet.tsx` · `home-layout-editor.tsx` |

**통합 점검 — 고친 뒤 한 번 더 독립 검토해 나온 것(같은 날)**

| # | 정한 것(기준) | 앱 쪽 자리 |
|---|---|---|
| 21 | **샘플 문장은 늘 위 줄**(버튼일 때도 알약일 때도) · 알약 옆은 오늘 합계. 알약 옆에 두면 샘플에서 그날 첫 기록을 저장할 때(버튼 → 알약) 카드가 한 줄 줄어듭니다(412 · 208 → 181). `CalendarStatusLines` 샘플 견본을 이 모양으로 | `calendar-card.tsx` · `gen_v5_sheets.sample_card` |
| 22 | **0건이면 알림 목록 · 자산 종합 점수도 판정하지 않는다**(8번과 같은 조건). 알림에서 월말 예상 판정 셋을 빼고, 점수는 `—` · `기록 전`(등급 없음). 첫날 다음 안내는 월급이 있으면 `달력에서 날짜를 눌러 쓴 돈을 적어 보세요` | `navi-commit-policy.basisAlerts` · `growth-score-card.tsx` · `app/page.tsx` · `navi-next-action.ts` |
| 23 | **소비 탭 기준 전 + 0건**: 위 카드의 사실 줄과 그래프 머리가 같은 말을 두 번 하지 않게 그래프 머리는 `누적 —`. **또래 띠 라벨**은 0건일 때 `내 목표와 비교하면`(값 `이번 달 기록이 아직 없어요`와 되풀이 없음) | `spending-analysis.tsx` · `peer-card.tsx` |
| 24 | **설명 줄 자리 예약.** 움직이는 동안 최종 · 출발 문장을 같은 칸에 숨겨 두어 세는 중의 짧은 꼴(`중 … 썼어요`)이 두 줄짜리 최종 꼴(`보다 … 더 썼어요`)로 바뀌어도 히어로 높이가 그대로(360 · 19px 흔들림 없앰). 움직임의 출발값이 월급과 같아 보이는 값이면(`173만원 중 1,725,900원`) 움직이는 동안에도 원 단위 | `home-hero.tsx` · `home-target.css` · `navi-hero.ts` |
| 25 | **오늘 `+`(이체 · ✓ 없음)는 합계 + 간격 + 수식어 줄을 합친 자리 가운데**(접힌 칸 24 · 큰 글자 29 · 월 달력 1.2em + 14 · 큰 글자 1.2em + 15) — 앱도 시안과 같게. 요일 · 날짜가 옆 칸과 같은 높이이고 첫 저장에 움직이지 않습니다. 접힌 칸의 `이체`(줄 12)는 9px 줄 안에서 1px 내림(border-box · padding-top 2 — 시안도 같게) | `v5-calendar.css` · `gen_calendar._mods` |
| 26 | 큰 글자 수식어 줄 13(시안과 같게 — 앱은 12였음 · 요일 · 날짜가 0.5px 낮았다). `CalendarStatusLines` 1540 → **1570**(샘플 견본이 한 줄 늘어 자연 1543.5). 측정: 접힌 칸 요일 · 날짜 · `+` · `이체` 위치가 시안과 앱에서 같음(보통 · 큰 글자 · 0.00px 차) | `v5-calendar.css` |
| 27 | `목록으로 보기 ›` 버튼이 `확인할 내용` 행의 누르는 영역 위에 오게(position relative) | `v5-calendar.css` |

**19 · 시안만 고친 것(앱 기준에 맞춤)**

- `EmptyStates` B = 앱이 실제로 그리는 **기록 부족 · 월급 없음** 틀: 배지 없음 · 각주 두 줄(`아직 기록하지 않은 소비는 포함되지 않았어요` + `월말 예상에 쓸 지난 소비 기록이 아직 없어요`) · 칸 `—` `—` `—`. C = **예상 기준 통과 · 월급 0원** 틀: 배지 `입력하면 보여요` · 기록 부족 각주 없음 · 칸 `월 실수령 —` · `월말 예상 208만원` · `월말 예상 여유 —`(앱이 늘 그리는 칸 셋). F의 `월급을 넣지 않았을 때` 줄에 규칙 — 기록 부족(A2)이 월급 없음(A4)보다 먼저라 배지가 없고, 예상 기준을 통과한 달만 회색 배지. F 두 줄은 `·`로 시작하는 줄이 생기지 않게 줄을 나눔. 제목 줄에 `예상 기준 확인 전` · `예상 기준 통과`.
- `PeerStates` A 목표 줄 `월말에도 목표 60% 안이에요`(명세 원문 · 앱 · B와 같음).
- 또래 카드 각주 — `HomeScroll`(→ `HomeDefaultScroll`) · `DesktopHomeV5`를 `PeerStates` C · 앱 문구로: `내가 직접 등록한 기준입니다. 앱에 내장된 통계나 순위가 아니에요.` / `출처 직접 입력한 예시 기준 · 2025년 · 표본 1,200명 · 실수령 급여 대비`(keep-all). 옛 `DesktopHome`(원본 전용 · v3 기록)은 그대로.
- `CalendarStatusLines` — `아직 오지 않은 날을 눌렀을 때` 견본에 `확인할 내용 2개` 행을 되돌림(안내가 떠 있는 동안에도 행은 그대로 — 앱과 같음) · 구현 메모 1(`버튼(40)이`를 한 덩어리로) · 2(UI 문구를 「」로) · 3(안내 문장은 알약 옆 · 줄 40 · 합계 줄과 행은 그대로).
- `HomeCalendar` — 3일 칸 누름(2px 링)을 그리지 않음. 오늘 칸도 1.5px 링이라 정지 그림에서 둘째 오늘처럼 읽혔습니다. 누름 모양은 `SPEC-COMPONENTS` §27-1 표 · `CalendarCells` 끝 문단 · `Components` 08 설명 1에 글로 남음. 캔버스 제목 · 홈 페이지 노트에서도 `3일 칸 누름`을 뗌(`Components` 08의 월 카드도 따라옴).
- `Components` 03b 그래프 규칙의 실선을 달력 자료대로(1일 101만 → 8일 112만 · `Spending` · `DesktopHomeV5`와 같은 곡선) · 08 칸 견본 일곱 개를 4 + 3 격자로(`오늘 · 안 적음`이 열을 넘치고 `포커스`만 둘째 줄로 떨어지던 것) · 설명 1에 수식어 줄 9 · 이체와 함께인 `+` 16.
- keep-all / nowrap — 첫 실행 안내 카드 본문(33장 + 다크 · `빠져나|가는` · `계획선보|다` 같은 끊김) · `Spending` 그래프 주석 · `CalendarGridSizes` 구현 메모 · `DesktopLedger` 주석 · `InsufficientElsewhere` 메모 1(`‘잠정’을`) · `DoneCardStates` 메모 2(`값)의`) · 또래 각주. 카드 · 문단 높이와 투어 좌표는 그대로(33장 카드 좌표 대조).
- `CalendarGridSizes` 목록 견본의 오늘 행(위 16) · 구현 메모 한 문장. `DoneCardStates` 6순위 행 `그 밖에 · 월급 있음 · 저장 전 기록 1건 이상` · 메모 2(위 9). `InsufficientElsewhere` 메모 2 · 6 · 끝 문단(위 8).
- 스트립 칸(위 13 · 14): 모든 스트립 칸의 수식어 줄 9 · 이체 · ✓와 함께인 오늘 `+` 16. 그냥 `+`(20)는 합계 + 수식어 자리 가운데에 두어 요일 · 날짜가 옆 칸과 같은 높이(원 위치는 전과 같음 · 전에는 옆 칸보다 3.5px 낮았음). 월 달력 칸도 같은 방식(이체와 함께면 원 16을 합계 자리에 겹침 · 날짜 줄이 옆 칸과 같은 높이).

**높이(최종 점검 후속 · 실제 웹폰트 자연 높이 + 24 이상):** `CalendarStatusLines` 1500 → **1540**(자연 1506) · `DoneCardStates` 1090 → **1100**(1074) · `EmptyStates` 2680 → **2800**(2766) · `InsufficientElsewhere` 1350 → **1390**(1360) · `HomeDefaultScroll` 1330 → **1360**(1330). 다크 짝도 같음 · 루트 · `gen_canvas.py` · `screens.json` 세 곳 같음(`sync_screens.py` 통과). 나머지 장(`Components` 2896 · `CalendarGridSizes` 1639 · `CalendarCells` 801 · `DaySheetNoSpendStates` 1164 · `DesktopHomeV5` 1039 · `PeerStates` 1511)은 그대로 들어갑니다. 홈 달력 카드 높이(222.4)와 투어 좌표는 그대로(칸 56 불변).

**닿은 장 — 라이트 53 · 다크 53**(첫 실행 안내 33장 포함 — 본문 keep-all만): `HomeCalendarStrip` · `HomeConfigured` · `HomeDefaultScroll` · `HomeStocksOff` · `HomeStocksCard` · `DoneCard` · `HeroInsufficient` · `HomeCalendar` · `CalendarCells` · `CalendarGridSizes` · `CalendarStatusLines` · `DaySheetNoSpendStates` · `DoneCardStates` · `InsufficientElsewhere` · `EmptyStates` · `PeerStates` · `Spending` · `DesktopHomeV5` · `DesktopLedger` · `Components` · `Tour*` 33장, 원본 전용 `HomeScroll`. 고친 생성기: `gen_calendar` · `gen_v5` · `gen_v5_sheets` · `gen_v5_screens` · `gen_tour` · `refresh_components_v5` · `gen_canvas`. 손편집: `EmptyStates` · `PeerStates` · `HomeScroll` · `DesktopHomeV5` · `Components`(03b) · `Spending` · `DesktopLedger`.
