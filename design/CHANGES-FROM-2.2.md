# 원본(2.2)과 달라진 점

정렬 · 글자 크기 · 정보 순서 · 버튼 위치를 기준으로 정리했습니다. 마지막 표에 **이동한 기능의 새 위치**가 있습니다.

---

## 1. 정렬

| | 2.2 | v3 |
|---|---|---|
| 요약 타일 | 라벨(가운데) / 큰 숫자(가운데) / 캡션(가운데, 2줄로 깨짐) | 이름 **왼쪽** · 금액 **오른쪽 정렬 tabular** · 비중 오른쪽 · ⋯ 메뉴 |
| 주지표 숫자 | 카드 가운데 | 카드 **왼쪽**, 오른쪽에 증감(`목표까지 2.1%p 여유`)을 나란히 |
| 목록 금액 | 데스크톱만 오른쪽 열, 모바일은 두 번째 행 | 모바일·데스크톱 모두 **오른쪽 고정폭 열** (모바일 74–96px) |
| 카드 헤더 | 제목·버튼·설명이 제각각 | eyebrow(왼쪽) + 상태 배지/링크(오른쪽) 한 줄로 고정 |
| 그래프 라벨 | Y축 눈금 열이 왼쪽 폭을 잠식 | 축 눈금 제거, 값은 데이터 옆 인라인 |

**왜:** 자릿수가 다른 금액은 오른쪽 정렬 열에서만 눈으로 비교됩니다. 가운데 정렬은 히어로의 결과값 하나에만 남기고, 그마저도 왼쪽으로 옮겨 증감과 짝지었습니다.

## 2. 글자 크기

| | 2.2 | v3 |
|---|---|---|
| 단계 수 | 17·18·20·21·23·26·32px 등이 혼재, 후반 CSS 재정의로 위계 역전 | **9단계 고정** (display / metric-xl / metric / title / section / body / label / meta / eyebrow) |
| 화면당 큰 숫자 | 32px 숫자가 4~5개 (자산 5타일, 목표 4타일, 미래 4타일) | `display` 54–62px는 **화면당 하나**, 나머지는 metric 18–21px 이하 |
| 단위 표기 | 값과 같은 크기인 경우 존재 | 항상 값보다 한 단계 작고 `ink-2` |
| 모바일↔데스크톱 | 화면마다 다른 축소 규칙(20/21/23/26px 예외) | display·metric-xl·metric·title 넷만 커짐 |

**왜:** 같은 크기 숫자가 여러 개면 어느 것도 주인공이 아닙니다. 화면마다 "지금 봐야 할 하나"를 정했습니다 — 홈 = 소비율, 자산 = 순자산, 소비 = 계획 대비 속도, 목적지 = 월 배분, 미래 = 10년 뒤 순자산.

## 3. 정보 순서

### 홈

| 순서 | 2.2 | v3 |
|---|---|---|
| 1 | 소비율 57.9% (가운데) | **현재 위치** 57.9% + 목표까지 여유, 같은 줄 오른쪽에 **순자산 대비 2.2%** (보조) |
| 2 | 목표 게이지 | **항로 바** — 진행 + 목적지 깃발(목표 60%) + 0/100% 눈금 |
| 3 | 급여 / 예상 소비 2개 | 월 실수령 / 월말 예상 / **남은 여유** 3개 |
| 4 | CTA 2개 나란히 | 근거 문장 + `기준 조정›`, 그 아래 주 행동 1개 |
| 5 | 소비 가정 펼치기 | **다음 안내** (내비 턴 카드) |
| 6 | 또래 카드 | 대표 목적지 (링 + 도착 예상) |
| 7 | 대표 목표 | 이번 달 한도 요약 |
| 8 | 보조 지표 3개 | *(스크롤)* 소비 가정 → 또래 → 더 깊이 보기 3행 |

첫 화면(390×844)에 **현재 위치 → 항로 → 다음 안내 → 목적지 → 한도**가 모두 들어갑니다.

**두 개의 분모.** 히어로의 주 지표는 `이번 달 예상 소비 ÷ 월 실수령 급여`(57.9%) 하나입니다.
`이번 달 예상 소비 ÷ 순자산`(2.2%)은 같은 캡션 줄의 오른쪽 끝에 11px 라벨 + 13px 값 + 셰브런으로 붙입니다.
주 지표는 54px, 보조는 13px — **4배 차이**라 두 숫자를 헷갈릴 여지가 없고, 캡션 줄 안에 들어가므로 히어로 높이는 그대로입니다.
셰브런은 스크롤 아래 `더 깊이 보기`의 순자산 대비 행으로 이어집니다.
급여를 아직 넣지 않아 주 지표가 `—`인 달에도 순자산 대비는 계산되므로, 미입력 카드에서 그 값을 함께 보여 줍니다(`상태 · 미입력 5종` B).
데스크톱에서는 오른쪽에 3지표 스트립이 있어 캡션 줄을 더 늘리면 스트립이 눌립니다. 그래서 `순자산 대비 2.2% ›`를 캡션 **바로 아래 줄**에 12px로 놓았습니다 — 왼쪽 열이 세로로만 자라므로 스트립 폭은 그대로입니다.

### 자산

2.2는 순자산 → 총자산/총부채 → 비상금/현금+투자 = **가운데 정렬 타일 5개**였습니다.
v3는 히어로 하나(순자산 + 6개월 스파크라인 + 순자산/부채 구성 막대)에 그 정보를 모으고, 그 아래 칩 2개, 그리고 **계좌 목록**이 바로 옵니다. 타일 5개가 차지하던 자리에 실제 계좌 5행이 들어갑니다.

### 소비

2.2는 소비율 타일 → 누적/월말 타일 → 총수입 대비/남는 비율 타일 → *(한참 아래)* 그래프였습니다.
v3는 **하나의 히어로 카드**에 소비율 + 상태 + 계획 대비 그래프를 함께 넣습니다. 총수입 대비 지표는 홈의 `더 깊이 보기`와 내역 탭 요약으로 옮겼습니다.

### 목적지 · 미래

목적지는 요약 타일 4개 → **월 저축 배분 카드 하나**(배분액 + 필요액 + 부족 안내)로 합치고 목록을 위로 올렸습니다.
미래는 타일 4개 → **기간·투자 환경 칩을 그래프 바로 위**에 두고 그래프를 히어로로, 그 아래 `다음 자산 지점` 행 목록으로 바꿨습니다.

## 4. 버튼 위치

| 버튼 | 2.2 위치 | v3 위치 | 이유 |
|---|---|---|---|
| **한도 조정** | 카드 헤더에서 제목과 같은 줄 (총한도 + 카테고리를 한 버튼이 담당) | **둘로 분리** — `총한도 조정`은 총한도 숫자 바로 아래 full-width, `배분 편집`은 카테고리 목록 헤더 | 하나의 버튼이 두 종류의 값을 바꾸지 않게 |
| **목표(목적지) 추가** | 탭 **위**에 떠 있음 | 목록 **마지막**의 점선 버튼 | 새 항목이 생길 자리에서 추가 |
| **월급·목표 조정** | 홈 히어로 안에서 주 CTA와 나란히 경쟁 | `실수령 급여 기준 …` 문장 **오른쪽**의 텍스트 링크 `기준 조정›` | 무엇이 바뀌는지 읽은 자리에서 누름 |
| **소비 기록하기** | 히어로 안 좌측 절반 | 히어로 **하단 full-width 단독** | 화면의 주 행동이 하나임을 분명히 |
| **자산 추가** | 화면 제목 아래 별도 행 | `자산 구성` 섹션 **헤더 오른쪽** soft 버튼 | 추가 대상 목록 바로 위 |
| **적립하기** | 목표 상세로 들어가야 함 | 목표 **행 안쪽 오른쪽** | 적립 불가한 순자산·부채 목표에는 **버튼 자체를 두지 않음** |
| **거래 추가** | 월 선택기 옆 큰 버튼 | 헤더 오른쪽 컴팩트 버튼(월 스테퍼와 한 줄) | 세로 공간 회수 |
| **자동 배분 초기화** | `자동 배분` | 카테고리 배분 헤더의 `자동으로 되돌리기` | 되돌리기임을 문구로 |

## 5. 그래프

| | 2.2 | v3 |
|---|---|---|
| Y축 | `140만/105만/70만/35만/0` 눈금 열 | 제거. 필요한 지점에만 옅은 기준선 + 인라인 라벨 |
| 가로 활용 | 8일 시점 누적선이 왼쪽 26%만 사용, 나머지 74% 공백 | 오늘까지 실선 + **월말까지 점선 예상선**으로 폭 전체 사용 |
| 값 읽기 | 툴팁 필요 | 오늘 지점 `112만`, 끝점 `월말 예상 208만` 라벨 상시 표시 |
| 높이 | 미래 차트 부모 CSS 280px vs 내부 `minHeight 300` 충돌 | 고정값 하나로 통일 (추이 150 / 미래 150 / 스파크라인 58) |
| 월별 막대 | 별도 막대 차트 (레이블 3개만 표시) | 자산 화면 스파크라인으로 통합, 소비는 누적선 하나로 |

## 6. 데스크톱

| | 2.2 | v3 |
|---|---|---|
| 구조 | 사이드바 224px + 가운데 한 열 (히어로 안이 비어 보임) | 사이드바 232px + 본문 **1.6fr / 1fr 두 열** |
| 왼쪽 열 | — | 주지표 히어로(숫자 왼쪽 · 보조 3개 오른쪽 · 항로 바가 카드 폭 전체) + 그래프 |
| 오른쪽 레일 | — | 다음 안내 · 대표 목적지 · 또래 |
| 사이드바 | 메뉴 이름만 | 각 탭의 **현재 수치**(자산 9,350만 / 소비 57.9% / 미래 2.0억) + `이번 달 남은 여유` 미니 카드 |
| 목록형 화면 | 동일 골격 | 목록 1.75fr / 요약 레일 1fr |

## 7. 색과 표현

- **카드 배경 그라디언트 12종 제거.** `color-mix` 기반 히어로/요약/시나리오 그라디언트를 없애고 **1px 테두리 + 최소 그림자**로 바꿨습니다. 흐릿한 큰 그림자 대신 선명한 테두리가 밀도를 만듭니다.
- **보라 = 저장되지 않는 가정**을 규칙으로 고정했습니다. 홈 소비 시나리오 카드는 보라 점선 테두리 + `저장되지 않는 가정` 배지 + `가정 종료`를 항상 함께 답니다. 미저장 상환 계획, 목표 계산기도 같은 표현입니다.
- **미입력은 회색 `—`.** 급여 미입력 홈, 자산 미입력, 급여 0원 + 부수입만 있는 상태를 각각 설계했습니다(`EmptyStates` 아트보드). 0원·0%·초록으로 바꾸지 않습니다.
- **주황과 빨강을 구분.** 주황 = 내 목표 초과, 빨강 = 급여 100% 초과.
- 카테고리 15색·자산 5색은 **2.2 값 그대로**입니다.

## 8. 또래 비교

| | 2.2 | v3 |
|---|---|---|
| 표현 | 40px 가운데 정렬 %p 숫자 + 막대 | **내 소비율 / 등록한 기준** 위아래 두 막대 |
| 없음 상태 | 안내 문단 | 5가지 상태를 각각 설계 (나이 미입력 / 기준 없음 / 비교 가능 / 급여·소비 부족 / 연도 확인 필요) |
| 출처 | 펼침 안에 | 카드 안 고정 — `내가 직접 등록한 기준입니다. 앱에 내장된 통계나 순위가 아니에요` + 출처·연도·표본수 |
| 비교 불가 시 | 안내만 | 개인 목표 대비 상태 + 다음 행동을 항상 남김 |

연령 구간(10년 단위 0~3 초반 / 4~6 중반 / 7~9 후반)은 그대로입니다.

---

## 9. 오류와 저장 상태

2.2는 오류를 대부분 "저장 안 됨" 한 줄로 알렸습니다. v3는 오류마다 **무엇이 잘못됐는지 · 어떻게 고치는지 · 입력한 값은 어떻게 되는지** 세 가지를 같이 말합니다(`상태 · 모달 오류·저장 6종`).

| 상태 | 2.2 | v3 |
|---|---|---|
| 필수 값 미입력 | 저장 후 실패 | 필드 빨간 테두리 + 이유 한 줄 + **저장 버튼 비활성**. "0원과 미입력은 다릅니다"를 카드 안에서 설명 |
| 값이 규칙 위반 | 저장 거부 | 초과량(20만원)을 막대로 보이고, **고르면 바로 해결되는 선택지 2개**를 결과값과 함께 제시 |
| 저장 중 | 없음 | 입력 영역을 흐리게 잠그고 버튼에 스피너. 취소도 비활성 |
| 저장 실패 | 토스트 한 줄 | **"입력한 값은 화면에 그대로 있으니"**를 본문에 명시 + 다시 저장 / 백업 내보내기 |
| 저장 완료 | 토스트 | 무엇이 어떻게 바뀌었는지(남은 한도 103만원) + 되돌리기 + 자동 닫힘 진행선 |
| 저장 없이 닫기 | 즉시 닫힘 | 확인 다이얼로그. 파괴적 행동만 빨간 버튼 |

## 10. 목적지 유형과 지난 달

### 목적지 5종 — 유형이 바꾸는 것은 셋뿐

| | 수익률 | 현재값을 어디서 | 적립 버튼 |
|---|---|---|---|
| 일반 저축 | 칸 없음 | 직접 입력 | 있음 |
| 비상금 | 0% 고정(잠김) | 직접 입력 | 있음 |
| 투자 | 연 기대수익률 입력 · **보라** | 직접 입력 | 있음 |
| 순자산 | 자산별 가중 수익률 · 자동 | **자산 화면에서 자동** | **없음** |
| 부채 상환 | 부채 금리 | **상환 전략 화면의 계획** | **없음** |

2.2는 다섯 유형이 같은 폼을 쓰면서 쓰지 않는 칸까지 다 보여 줬습니다. v3는 **그 유형이 쓰지 않는 칸을 아예 지웁니다.** 순자산·부채 목표에 적립 버튼이 없는 이유도 카드 안에서 한 줄로 설명합니다 — 값이 다른 화면에서 오기 때문입니다.

### 적립액 추가 — 무엇이 바뀌는지 먼저

저장 전에 **적립액 · 진행률 · 도착 예상**이 어떻게 바뀌는지 세 줄로 보여 줍니다. "목적지의 적립액만 바꾸고 통장 잔액에는 중복 반영되지 않는다"는 문구를 설명이 아니라 카드 안에 둡니다.

### 지난 달 — 예상 말투를 전부 없앰

| 이번 달 | 지난 달 |
|---|---|
| 월급 대비 **예상** 소비 | 월급 대비 소비 **확정** |
| 월말 예상 | 월 소비 합계 |
| 실선 + 점선 예상 | 실선만 |
| 탭 3개(이번 달·내역·한도) | **탭 2개** — 한도는 이번 달을 계획하는 도구 |

없어진 한도 탭은 탭 줄 바로 아래 한 줄로 설명합니다. 없는 것은 없는 자리에서 알려 주는 게 카드 하나를 더 두는 것보다 낫습니다.

### 코칭 · 기록 없음

"안내할 것이 없다"에서 끝내지 않고 **무엇이 있으면 시작되는지**(급여 + 소비 3건)를 진행 단계로 보여 주고, 쌓이면 받게 될 안내를 흐린 예시로 미리 봅니다. 지어낸 일반 조언을 넣지 않는다는 원칙을 화면에 적었습니다.

## 11. 이동한 기능의 새 위치

| 기능 | 2.2 위치 | v3 위치 |
|---|---|---|
| 총한도 조정 | 소비 › 한도 › 카드 헤더 `한도 조정` | 소비 › 한도 › **총한도 카드 하단** `총한도 조정` |
| 카테고리 배분 | 위 버튼과 같은 모달 | 소비 › 한도 › **카테고리 배분 헤더** `배분 편집` (같은 모달의 배분 섹션으로 진입) |
| 카테고리 자동 초기화 | 모달 상단 `자동 배분` | 모달 › 카테고리 배분 헤더 `자동으로 되돌리기` |
| 월급·소비 목표 조정 | 홈 히어로 `월급·목표 조정` 버튼 | 홈 히어로 하단 근거 문장 오른쪽 `기준 조정›` (같은 기본 입력 모달) |
| 총수입 대비 소비 | 소비 › 이번 달 상단 타일 | 홈 › `더 깊이 보기` 행 + 소비 › 내역 상단 요약 줄 |
| 순자산 대비 소비 | 홈 보조 타일 | 홈 › `더 깊이 보기` 행 (펼치면 상세) |
| 자산 종합 점수 | 홈 펼침 상세 | 홈 › `더 깊이 보기` 행 |
| 10년 뒤 순자산 | 홈 펼침 상세 | 홈 › `더 깊이 보기` 행 + 미래 탭 히어로 |
| 현금성 비상금 / 현금+투자 | 자산 요약 타일 2개 | 자산 › 히어로 아래 **칩 2개** |
| 부채/자산 비율 | 자산 요약 타일 캡션 | 자산 › 히어로 **구성 막대 아래 라벨** |
| 미래 기간·투자 환경 선택 | 그래프 아래 컨트롤 블록 | 미래 › **그래프 바로 위 칩 행** |
| 다음 자산 지점 | 예측 가정 펼침 안 | 미래 › 그래프 아래 **독립 카드**(3행) |
| 반복 거래 관리 | 소비 › 내역 › 하단 링크 | 소비 › 내역 › **검색줄 오른쪽 버튼** + 데스크톱 오른쪽 레일 카드 |
| 적립하기 | 목표 상세 진입 후 | 목적지 › **목표 행 오른쪽** `+ 적립` |
| 홈 대표 지정 | 목표 행 메뉴 | 목표 행의 `홈 대표` 배지 + 행 메뉴(유지) |
| 나이 수정 | 또래 카드 헤더 `나이 수정` | 유지 — 또래 카드 헤더 오른쪽 (`기준 수정`으로 통합) |
| 소비 시나리오 | 홈 › 접힌 상세 | 유지 — 홈 스크롤 영역, 보라 가정 카드로 시각 구분 |
| 월 마감(실제 수치 확정) | 소비 › 과거 월 | 유지 — 소비 › 과거 월 선택 시 상단 배너 + 모달 |
| 백업 내보내기/가져오기 | 설정 모달 하단 | 유지 — 설정 · 내 수치 모달 하단 |

**탐색 구조는 5개 그대로**입니다(홈 · 자산 · 소비 · 목적지 · 미래). 내부 서브탭도 그대로입니다. 화면 이름만 `목표` → `목적지`로 바꿔 내비게이션 은유와 맞췄습니다(하단 탭 라벨은 `목표` 유지 가능 — 아트보드에서는 탭 라벨 `목표`, 화면 제목 `목적지`로 그렸습니다).
