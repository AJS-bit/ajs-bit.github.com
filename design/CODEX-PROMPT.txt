NAVI 앱의 UI를 v3 디자인으로 구현해 주세요. 디자인 자료는 아래 공개 저장소에 있습니다.

  https://raw.githubusercontent.com/AJS-bit/ajs-bit.github.com/claude/navi-ui-ux-redesign-nzxoxz/design/CODEX-BRIEF.md

먼저 이 CODEX-BRIEF.md를 처음부터 끝까지 읽으세요.
읽는 순서, 작업 순서, 바꾸면 안 되는 것, 완료 판정 기준이 전부 그 안에 있습니다.

같은 경로의 design/ 아래에서 필요한 파일을 그때그때 raw로 받아 쓰면 됩니다.
그 저장소에는 커밋하지 마세요. 읽기 전용 참고 자료입니다.

미리 알아 둘 것만:

- 시안 .dc.html의 인라인 스타일 값을 **그대로 가져오세요.**
  13.5px는 text-sm(14px)이 아니고, border-radius 18px는 rounded-2xl(16px)이 아닙니다.
  "비슷한 Tailwind 클래스"로 바꾸면 시안과 다른 화면이 나옵니다.
  토큰은 값에 이름을 붙이는 용도이지 값을 반올림하라는 뜻이 아닙니다.
  아트보드와 문서가 어긋나면 아트보드가 기준입니다.

- 화면보다 **컴포넌트를 먼저** 만드세요. design/SPEC-COMPONENTS.md에 24개의 실측 CSS가 있습니다.
  화면부터 손대면 화면마다 조금씩 다른 카드가 생깁니다.

- 화면 하나를 만들 때 여는 파일은 네 개입니다.
    design/SPEC-SCREENS.md 의 해당 절   → 블록 순서 체크리스트
    design/canvas/<이름>.dc.html        → 정확한 값 (최종 기준, 처음부터 끝까지 읽으세요)
    design/spec/<이름>.outline.md       → 위 파일의 구조 요약
    design/preview/<이름>.png           → 결과가 어떻게 보여야 하는지

- 만든 화면은 CODEX-BRIEF.md의 "3-1. 자가 점검"을 돌린 뒤에 다음 화면으로 가세요.
  눈으로 "비슷하네" 하고 넘어가면 반드시 어긋납니다.

- 계산 로직·거래 ID·백업 형식·카테고리 색은 절대 바꾸지 마세요.
  디자인 때문에 계산을 바꿔야 한다면 멈추고 물어보세요. 그 디자인이 틀린 것입니다.

- 로그인·클라우드·외부 API를 새 요구사항으로 만들지 마세요. 외부 서비스 없이 동작해야 합니다.

- canvas/_tools/ 의 생성기(gen_*.py)는 다시 돌리지 마세요. 시안은 확정본입니다.
  단 darken.py의 MAP(다크 색 대응표)과 render_png.py(캡처 도구)는 읽고 써도 됩니다.

CODEX-BRIEF.md의 4단계를 순서대로, 화면 하나씩 끝내고 다음으로 가세요.
1단계(토큰 교체)만 끝나도 되돌릴 수 있는 상태여야 합니다.
