# 여기부터 읽으세요

NAVI v3 디자인 인계 묶음입니다. 원본 앱 소스는 들어 있지 않습니다 — 그건 여러분 저장소에 있습니다.

zip으로 받았든 GitHub에서 보고 있든 내용은 같습니다.

```
https://github.com/AJS-bit/ajs-bit.github.com/tree/claude/navi-ui-ux-redesign-nzxoxz/design
```

## 코덱스에게 넘길 때

`CODEX-PROMPT.txt`의 내용을 그대로 붙여넣으세요. 그 다음 코덱스가 읽을 순서는
`CODEX-BRIEF.md`가 안내합니다.

## 사람이 먼저 볼 것

`preview/index.html`을 브라우저로 열면 아트보드 75장이 라이트/다크 짝으로 한눈에 보입니다.
`canvas/navi-redesign.html`을 열면 같은 것을 확대·이동 가능한 캔버스에서 봅니다.

## 폴더 구조

| 경로 | 무엇 |
|---|---|
| `CODEX-BRIEF.md` | **구현 지시서.** 읽는 순서·작업 순서·자가 점검·완료 판정 |
| `CODEX-PROMPT.txt` | 코덱스에 그대로 붙여넣는 첫 지시 |
| `SPEC-COMPONENTS.md` | 컴포넌트 24개의 실측 CSS (라이트·다크). **구현은 여기서 시작** |
| `SPEC-SCREENS.md` | 화면 38개의 블록 조립 체크리스트 |
| `spec/*.outline.md` | 아트보드별 블록 개요 38장 |
| `canvas/*.dc.html` | **아트보드 75장 — 값의 최종 기준** |
| `preview/*.png` | 아트보드 75장 렌더 + `index.html` 색인 |
| `screens.json` | 아트보드 ↔ 원본 소스 파일 매핑 |
| `sample-data.json` | 시안이 그리는 가상 사용자. 개발 픽스처로 그대로 사용 |
| `tokens.v3.json` · `DESIGN-TOKENS-v3.md` | 색·타이포·간격 토큰 |
| `README.md` | 왜 이렇게 바꿨는지 — 문제 진단과 5가지 원칙 |
| `IMPLEMENTATION-PLAN.md` | 파일별 작업 내용 (4단계) |
| `CHANGES-FROM-2.2.md` | 2.2에서 무엇이 달라졌는지 |
| `canvas/_tools/` | 아트보드 생성기·렌더 도구·계산기. 생성기는 다시 돌리지 마세요 |

## 값이 서로 다르면

**`canvas/<이름>.dc.html`이 항상 기준입니다.** 문서·토큰·렌더가 아트보드와 다르면 아트보드가 맞습니다.

숫자(완제일·도착일·총이자)는 예외입니다. 그건 `canvas/_tools/calc/`의 계산기가 만든 값이고,
계산기와 원본 앱 엔진이 어긋나면 **엔진이 맞습니다.** `calc/crosscheck.py`가 둘을 대조합니다.
