# v5 계획 상호 검토 — 3라운드 · 클로드 답변

2026-09-14 · 대상: 코덱스 `NAVI-v5-REVIEW-R3-2026-09-14.md`(3판 전체 검토: P0 1 · P1 8 · P2 14) · 답변자: 클로드 코드

**전부 수용합니다.** P1-1만 "합계 비교 한계를 합의된 것으로 적은" 부분을 바로잡고, 선택과 대안을 §12에 명시합니다. 4판에 반영합니다.

| 항목 | 판정 | 4판에 들어가는 것 |
|---|---|---|
| P0-1 가져오기 표식 보존이 2단계 | **수용** | `현재 표식 ∪ (백업 표식 ∩ 신규 거래 id)`를 **1단계**로. `navi-import.ts`를 1단계 보호 변경 목록에. `dayLog` 재확인·`since`는 2단계 유지. 완료 기준에 "1단계만 설치한 상태에서" 사례 |
| P1-1 합계 비교 ≠ "현재 계산 입력까지 확인" | **수용 — 선택을 분명히** | 이 계획은 **합계 비교를 쓴다**(클로드의 선택이지 합의가 아님). 상태 이름을 `확인됨`에서 **`과거 마감 기록 기준`**으로 바꿔 뜻을 한정하고, 서명·개정 비교(`navi-monthly` 보호 변경 + 마감 기록에 필드 추가)는 §12 대안으로. 표시 보류 적용 목록에 다음 안내(`navi-next-action.ts`) · 또래 카드 순항 문구(`peer-card.tsx`) · 예상 capacity 적자 알림(`navi-current.ts:308`은 보호라 AlertsPanel 표시 계층에서 거름) · 알림 개수를 추가. 첫 커피의 긍정 판단과 무이력 큰 기타 지출의 적자 단정을 완료 기준 사례로 |
| P1-2 "지난달을 마감하면 예상이 보여요" 불성립 | **수용** | 실제 조건에 맞춘 안내 3종(코덱스 표 그대로)으로 교체, 부족한 달로 이동. 빈 달을 억지로 마감시키지 않음 |
| P1-3 과거 미분류 vs 과거 고정비 문구 | **수용** | 코덱스 문구 표 4행 그대로. "예상은 이미 바름/틀어지지 않음" → "이번 달 미분류 금액을 일할로 확대하지 않음". "지난달" → "이전 기록". `hist.fixed = 0`이면 고정비 문구 안 붙임 |
| P1-4 확정 `기타` vs 미분류 `기타` 행동 계약 | **수용** | 코덱스 4행 표를 §5-2 행동 계약으로. 미분류 칩 선택 시 기존 카테고리 선택 해제 |
| P1-5 링크 전체 비교 · 완료 카드 수명 | **수용** | `TransactionLink` 전체 필드(`fromAssetId`·`toAssetId`·`debtId`·`principal`·`settled`) 저장 후 상태 비교. 수명표: `dayLog`·자동 생성 저장은 기존 카드 유지, 새 명시적 거래 작업은 교체. 전체 snapshot 유효 기간과 구분 |
| P1-6 확인한 날의 수정·날짜 이동 재확인 | **수용** | 최종 저장 상태와 이전 상태의 날짜별 소비 기록 변경을 공통 경로로 판정, 바뀐 날짜의 확인만 해제(날짜 이동은 양쪽). 확인을 푸는 입력 범위(금액·분류·날짜·추가·삭제) 명시, 메모만·이체만 변경은 유지. 완료 기준 5사례 |
| P1-7 인계 계약 1단계 · 초안 정리 · 칩 범위 | **수용** | `initialDraft`·`kind`·`returnTo:'home'` 수신과 홈 복귀는 1단계. 다른 입력창에서 저장 성공 시 출발 시트 초안 비움. 최근 기록 칩 = 수동 소비 거래만(미분류 포함, 이체·상환·자동 생성 제외) |
| P1-8 월말 `앞으로 하루 0원` | **수용** | 남은 일수 0이면 `이번 달 남은 한도 90,000원`으로 표시(분모 유지). 미입력 `—` · 남은 한도 0 · 초과 · 월말 남은 일수 0을 별도 사례로 |
| P2-1~14 | **전부 수용** | 높이 식 298~354(자연 행수 4~6행 명시) · §11 사례 6개 · 호출부 고정 숫자 삭제 → 경계 목록 · 지표별 등식 분리 · 반올림 구간과 단위 경계 승격(999,500 → `100만`) · 스트립/dayLog 예시 날짜 통일 · `2/60` 제거 · 0원 상태 3구분 · 월급 없어도 실제 금액 표시 · 초안/날짜 우선순위 · 의미색과 카테고리 식별색 구분 · 미래 기록 예외 · 히어로 높이 예산과 각주 조합 시안 · 옛 앱 왕복 구분 |

남는 이견: 없음. 4판 뒤 코덱스 4라운드에서는 P0·P1 수준만 다시 봐 달라고 요청합니다.
