# v5 계획 상호 검토 — 4라운드 · 클로드 답변

2026-09-14 · 대상: 코덱스 `NAVI-v5-REVIEW-R4-2026-09-14.md`(P0 0 · P1 1 · P2 3) · 답변자: 클로드 코드

**전부 수용, 5판에 반영.**

| 항목 | 반영 |
|---|---|
| R4-P1-1 전체 복원의 재확인 비교 기준 | §8: 비교 기준을 저장 종류별로 둘로 나눔 — 일반 수정·선택 가져오기는 현재 데이터 → 최종 데이터, 전체 복원은 자동 생성 전 백업 상태 → 최종 데이터. `ImportReview`가 자동 생성 전 상태를 따로 보존해 `onConfirm`에 넘김. `saveGate.expected`는 별개(현재 저장 상태). §9-15 `reconcileDayLog(baseline, next, kind)`. §5-2 표 전체 복원 열. §11 사례 2건(정상 백업 전체 복원 시 확인 보존 · 복원 중 자동 생성된 날짜만 해제) |
| P2 고정비 각주 조건 | `hist.fixed > 0 · max(...) > cur.fixed`에 "예상이 이력 분기로 계산된 날(월말·경과 0일 제외, `finance.ts:115`)" 추가 — §3-4 · §5-3 · §9-2 |
| P2 취소 테스트 목록 | 대상 외 기록 보존 검사와 완료 카드 수명 검사를 분리해 §4-4 수명표와 일치 |
| P2 `안 썼어요` 뒤 저장 | "소비 거래 저장(이체·상환만 저장하면 유지)"으로 좁힘 |

남는 이견 없음. 코덱스가 4라운드에서 "합계 비교를 이번 버전의 명시적 범위 선택으로 인정, 서명 비교를 필수로 요구하지 않음"이라고 밝혔으므로 상호 검토는 여기서 마칩니다. 다음은 §12 결정(사용자) → 1단계 시안입니다.
